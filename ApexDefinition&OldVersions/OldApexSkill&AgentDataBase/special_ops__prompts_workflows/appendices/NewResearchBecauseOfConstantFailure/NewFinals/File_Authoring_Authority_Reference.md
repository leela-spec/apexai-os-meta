text

`--- version: "1.0.0" created_at: "2026-05-09" purpose: "Authoritative rules for writing, structuring, and validating any file loaded by a sub-agent" scope: "All files authored for or by orchestration sub-agents" file_types: [system_prompt, knowledge_base, workflow, tool_definition] ---`

# File Authoring Authority Reference

text

`# ══════════════════════════════════════════════════════ # SECTION 1 — CRITICAL DIRECTIVES # ══════════════════════════════════════════════════════ critical_directives:   - id: CD-01    tier: critical    directive: "Use YAML fenced blocks for all structured data; use Markdown prose only for human-facing explanatory text."   - id: CD-02    tier: critical    directive: "Assign every field a scalar type from: string | integer | boolean | enum | list | map."   - id: CD-03    tier: critical    directive: "Give every file a YAML frontmatter block as its first element containing: version, purpose, file_type, and schema_ref."   - id: CD-04    tier: critical    directive: "Place every required field before optional fields within any YAML block."   - id: CD-05    tier: critical    directive: "Write every key in snake_case; reject camelCase, PascalCase, and kebab-case keys."`

text

`# ══════════════════════════════════════════════════════ # SECTION 2 — FILE TYPE TAXONOMY # ══════════════════════════════════════════════════════ file_type_taxonomy:   system_prompt:    purpose: "Defines agent role and behavioral rules"    required_sections: [frontmatter, role, directives, stop_conditions]    optional_sections: [examples]    forbidden_content:      - knowledge_corpus      - workflow_steps      - tool_schemas   knowledge_base:    purpose: "Stores reference data and domain facts for lookup"    required_sections: [frontmatter, source_metadata, entries]    optional_sections: [index]    forbidden_content:      - behavioral_directives      - workflow_logic      - tool_schemas   workflow:    purpose: "Encodes ordered steps, branching, and handoff logic"    required_sections: [frontmatter, preconditions, steps, stop_conditions, escalation_path]    optional_sections: [decision_table]    forbidden_content:      - role_definitions      - knowledge_corpus      - tool_schemas   tool_definition:    purpose: "Declares tool interface: name, parameters, schema, errors"    required_sections: [frontmatter, tool_name, description, input_schema, output_schema, error_codes]    optional_sections: [usage_notes]    forbidden_content:      - behavioral_agent_rules      - knowledge_corpus      - workflow_steps`

text

`# ══════════════════════════════════════════════════════ # SECTION 3 — YAML SYNTAX RULES # ══════════════════════════════════════════════════════ yaml_syntax_rules:   - id: SYN-01    tier: critical    rule: "Indent with 2 spaces per level; never use tabs."   - id: SYN-02    tier: critical    rule: "Use true/false for booleans; never use yes/no/on/off."    correct: "enabled: true"    wrong:   "enabled: yes"   - id: SYN-03    tier: critical    rule: "Quote any string value that contains special characters, starts with a digit, or could be misread as a boolean or null."    correct: 'id: "0123"'    wrong:   "id: 0123  # parsed as integer"   - id: SYN-04    tier: required    rule: "Use the block scalar | for multi-line strings where newlines are meaningful; use > for flowing prose."   - id: SYN-05    tier: required    rule: "Declare every list item on its own line with a leading hyphen and single space; do not use flow style [ ] in instruction blocks."    correct: |      items:        - alpha        - beta    wrong: "items: [alpha, beta]"   - id: SYN-06    tier: required    rule: "Separate top-level YAML sections with one blank line; do not insert blank lines between sibling keys."   - id: SYN-07    tier: required    rule: "End every file with a single newline character; remove all trailing whitespace."   - id: SYN-08    tier: recommended    rule: "Anchor repeated structures with & and alias with *; never duplicate blocks by copy-paste."    correct: |      defaults: &defaults        timeout: 30      task_a:        <<: *defaults    wrong: |      task_a:        timeout: 30      task_b:        timeout: 30`

text

`# ══════════════════════════════════════════════════════ # SECTION 4 — FIELD TYPING RULES # ══════════════════════════════════════════════════════ field_typing_rules:   - id: FT-01    tier: critical    rule: "Declare the type of every field in the schema_ref document; never leave fields implicitly typed."   - id: FT-02    tier: critical    rule: "Use ISO 8601 format for all date and datetime values."    correct: 'created_at: "2026-05-09"'    wrong:   "created_at: May 9 2026"   - id: FT-03    tier: required    rule: "Define enum fields with an explicit allowed_values list in the schema; reject unlisted values at parse time."   - id: FT-04    tier: required    rule: "Express numeric IDs and version strings as quoted strings, not bare integers."    correct: 'id: "DR-01"'    wrong:   "id: DR-01"   - id: FT-05    tier: required    rule: "Mark every optional field in the schema with required: false; treat all unmarked fields as required: true."`

text

`# ══════════════════════════════════════════════════════ # SECTION 5 — STRUCTURE RULES # ══════════════════════════════════════════════════════ structure_rules:   - id: STR-01    tier: critical    rule: "Open every file with a YAML frontmatter block delimited by --- before and after."   - id: STR-02    tier: critical    rule: "Use fenced YAML code blocks (triple backtick yaml) for every structured section in a Markdown file."   - id: STR-03    tier: required    rule: "Name every fenced block section with a comment header on the first line inside the block."   - id: STR-04    tier: required    rule: "Keep nesting to a maximum of 4 levels; flatten deeper structures into referenced sub-files."   - id: STR-05    tier: required    rule: "Assign a unique ID to every named entry using the pattern PREFIX-NN."    correct: 'id: "STR-05"'    wrong:   "id: rule5"   - id: STR-06    tier: required    rule: "List all sections in the frontmatter sections field in the order they appear in the file."   - id: STR-07    tier: recommended    rule: "Add a schema_ref field in the frontmatter pointing to the JSON Schema file that validates this file."    correct: 'schema_ref: "schemas/workflow.schema.json"'`

text

`# ══════════════════════════════════════════════════════ # SECTION 6 — IF-THEN AUTHORING DECISIONS # ══════════════════════════════════════════════════════ authoring_decisions:   - id: AD-01    condition: "file contains more than one file_type of content"    action:    "split into separate typed files before committing"    fallback:  "mark file as draft and block loading"   - id: AD-02    condition: "a value could be parsed as boolean, null, or number by a YAML 1.1 parser"    action:    "wrap the value in double quotes"    fallback:  "add an inline comment stating the intended type"   - id: AD-03    condition: "a structure repeats more than twice"    action:    "extract to a YAML anchor and alias all instances"    fallback:  "extract to a shared include file referenced by ID"   - id: AD-04    condition: "nesting depth exceeds 4 levels"    action:    "flatten by extracting the nested block into a referenced sub-file"    fallback:  "refactor the data model to reduce hierarchy"   - id: AD-05    condition: "a field has no declared type in the schema"    action:    "add type declaration to schema before writing the value"    fallback:  "annotate with an inline YAML comment stating the intended type"   - id: AD-06    condition: "file fails any MVT check"    action:    "block the file from loading and return the failing check ID"    fallback:  "none — do not load a file that fails MVT"`

text

`# ══════════════════════════════════════════════════════ # SECTION 7 — ANTI-PATTERNS # ══════════════════════════════════════════════════════ anti_patterns:   - id: AP-01    pattern_name: "Unquoted Ambiguous Scalars"    what_fails: "Parser coerces values like yes, 0123, or null to unexpected types silently."    minimum_fix: "Quote all values that match boolean, integer, float, or null patterns."   - id: AP-02    pattern_name: "Mixed Content in One File"    what_fails: "A file holding multiple content types prevents targeted reloading and causes key collisions."    minimum_fix: "Split into separate files typed per FILE TYPE TAXONOMY."   - id: AP-03    pattern_name: "Inconsistent Key Naming"    what_fails: "Mixed camelCase and snake_case keys break programmatic parsing and schema validation."    minimum_fix: "Normalize all keys to snake_case; enforce with yamllint on commit."   - id: AP-04    pattern_name: "Implicit Typing"    what_fails: "Fields without declared types are parsed differently across YAML libraries and versions."    minimum_fix: "Declare a type for every field in the schema_ref document."   - id: AP-05    pattern_name: "Missing Frontmatter"    what_fails: "Files without frontmatter cannot be validated for type, version, or schema conformance before loading."    minimum_fix: "Add a YAML frontmatter block as the first element of every file."   - id: AP-06    pattern_name: "Flow Style in Instruction Blocks"    what_fails: "Inline flow style reduces readability and makes diff review unreliable."    minimum_fix: "Convert all flow-style structures to block style."   - id: AP-07    pattern_name: "Duplicate Keys"    what_fails: "YAML 1.1 and 1.2 parsers handle duplicate keys differently; data is silently lost."    minimum_fix: "Run yamllint with duplicate-keys rule enabled on every save."   - id: AP-08    pattern_name: "Deep Nesting"    what_fails: "Structures deeper than 4 levels are difficult to diff, validate, and parse without path errors."    minimum_fix: "Flatten to ≤4 levels; extract deeper structures into referenced sub-files."`

text

`# ══════════════════════════════════════════════════════ # SECTION 8 — MINIMAL VIABLE FILE TEST # ══════════════════════════════════════════════════════ minimal_viable_file_test:   - check_id: MVT-01    question: "Does the file open with a valid YAML frontmatter block containing version, purpose, file_type, and schema_ref?"    fail_action: "Add frontmatter before loading."   - check_id: MVT-02    question: "Do all keys use snake_case exclusively?"    fail_action: "Normalize keys to snake_case; re-validate."   - check_id: MVT-03    question: "Does the file contain content from exactly one file_type?"    fail_action: "Split into typed files; block loading until complete."   - check_id: MVT-04    question: "Does the file parse without errors under a strict YAML 1.2 parser with duplicate-key detection enabled?"    fail_action: "Fix all parse errors and duplicate keys before loading."`

text

`# ══════════════════════════════════════════════════════ # SECTION 9 — QUICK REFERENCE # ══════════════════════════════════════════════════════ quick_reference:   key_conventions:    case: snake_case    id_pattern: "PREFIX-NN  (e.g. CD-01, SYN-03, AP-07)"    date_format: "ISO 8601  (YYYY-MM-DD)"   indentation:    unit: 2 spaces    tabs: forbidden   boolean_values:    allowed:  [true, false]    forbidden: [yes, no, on, off, True, False, TRUE, FALSE]   string_quoting:    double_quotes: "values with escape sequences or special characters"    single_quotes: "literal values, no escape processing"    no_quotes:     "simple alphanumeric values only"   block_scalars:    "|": "literal — preserves newlines"    ">": "folded — newlines become spaces"   max_nesting_depth: 4   required_frontmatter_fields: [version, purpose, file_type, schema_ref]   file_types: [system_prompt, knowledge_base, workflow, tool_definition]   forbidden_patterns:    - tabs in indentation    - camelCase or PascalCase or kebab-case keys    - yes/no/on/off as boolean values    - unquoted ambiguous scalars    - duplicate keys in any block    - flow style in instruction blocks    - nesting deeper than 4 levels    - missing frontmatter    - mixed file_type content in one file    - fields without declared types in schema`