---
title: "Simulation record — US-IDEA-01 (first real run of the orchestration system)"
story: "US-IDEA-01 — Raw Voice Idea to Knowledge and Project Action (user-stories.md §5.5)"
run_folder: "US-IDEA-01-20260711/"
date: 2026-07-11/12
verdict: "PASS through stage 5 (review complete, artifact verified); stage 6 mutation correctly HELD at the operator gate — completion condition requires real operator acceptance, which cannot be simulated"
---

# US-IDEA-01 — first real run

## What actually happened (real tool calls, real files, real subagents)

| Stage (story) | Executed by | Real mechanism | Artifact |
|---|---|---|---|
| 1 Preserve before interpreting | Alfred (main-conv contract) | verbatim operator message captured immutable, voice-typo resolution recorded as metadata not edit | `00-intake-packet.md` |
| 2 Candidate structured entry | Meta Ops (main-conv contract) | distillation with confidence-marked claims, preserved roughness, bounded actions incl. null action | `01-candidate-entry.md` (v1) |
| 3 Placement & lifecycle check | **spawned `knowledge-bank` agent** | real repo greps; classification EXTENSION; one primary home recommended; **caught a real defect**: integration note references "v3 prompts" that don't exist on disk | `02-placement-packet.md` |
| (6-support) Deterministic checks | Meta Ops | **real `apex_sync.py` runs**: registry preview (8 tasks), drift report (`drift_detected: true`, registry never materialized); write held per two-step rule | `03-sync-computed-packet.md` |
| 4 Independent fidelity review | **two spawned `meta-detective` agents, blind, parallel** | validity lens: C1/C2/C3 pass, **C4 revise — caught two real pointer defects** (§7→§8 misattribution, miscased filename); alignment lens: A1/A2/A3 pass | `04`-`07` |
| 4-repair | source_owner (Meta Ops) | v2 with exactly the two corrections, **diff-proven**, new hash locked; **scoped C4 re-review by a fresh detective: pass** (both defects verified fixed against real files) | `01-candidate-entry.v2.md`, `08-promotion-gate.md` |
| 5 Durable-knowledge gate | Alfred | gate presentation with G1 (accept+A1 / accept+A2 / edit / reject / hold) and G2 (registry write) | `09-gate-presentation.md` |
| 6 Accepted state mutation | — | **correctly NOT executed** — `operator_validation: not_requested`; no mutation without the operator | — |

## Verdict: PASS (stages 1–5), gate pending — honest reading

The story's completion condition ("Alex accepts, edits, or rejects durable mutation") requires the real operator; everything the system itself owns ran for real and behaved per contract. Strongest evidence the system works: **the review loop caught genuine defects the author missed** (wrong section number, wrong filename, phantom v3 reference) and the deterministic aggregation forced a bounded, diff-proven correction cycle instead of a hand-wave.

## Defects found IN THE SYSTEM by this run (design lessons, all candidates)

- **DL-1 (schema defect, real):** `authority` fields live in artifact frontmatter, but promoting candidate→verified would edit the artifact and change its hash, invalidating every verdict binding. Worked around via sidecar (08). Fix direction for the enforcement-code build item: define `basis_digest` over content *excluding* the authority block, or keep authority in a sidecar permanently. (ARCHITECTURE §7.1)
- **DL-2 (tooling gap, real):** read-only reviewers (`meta-detective`, tools Read/Grep/Glob) cannot recompute hashes — both lenses verified identity by lock-attestation only and honestly flagged it. Orchestrator recomputed post-hoc. Options: accept attestation+orchestrator-recompute as the pattern (current), or grant reviewers a hash utility (tool-scope escalation — needs an observed failure to justify).
- **DL-3 (process observation):** the blind-parallel protocol held — alignment reviewer explicitly declined to judge a fidelity question it noticed ("belongs to the other lens's scope, which I did not and may not consult"). The boundary language in the agent definitions is doing real work.
- **DL-4 (stale-reference class):** two of three citation defects were of the exact class the idea record itself is about (imprecise pointers). Corroborates the record's value.

## Regression note

Any change to schemas/, workflows/, .claude/agents/, or the three apex skills re-runs this story. The run folder is the fixture; the gate answers (G1/G2) belong to the operator turn that follows.
