# Handover — Fable Orchestration completion plan, 2026-07-12 session close

Read this file, then `Newplan.md` (the operative plan), before doing anything else. Do not start
Iteration 4 until the one open verification item below is resolved.

## Where things stand

- **Branch:** everything lives on `main` now. The operator merged `claude/fable-orchestrator-setup-9pc5pu`
  into `main` this session (all future commits go to `main`; the branch-only push instruction in the
  original uploaded plan is stale and superseded — `Newplan.md` already reflects this).
- **Repo state:** working tree clean, all work below is committed to `main`. Not yet pushed to
  `origin/main` as of session close — confirm with the operator before pushing.
- **The plan to follow:** `apex-meta/fable-orchestrator/Newplan.md`. It has three sections that matter
  most: "Model routing per iteration" (which model/effort per iteration, plus the stop-and-switch
  protocol), "Doctrine efficiency work" (what changed and the one open item — see below), and
  "Remaining work — iteration order" (Iterations 3-8).

## Iteration status

| Iteration | Status |
|---|---|
| 1 | Done (verified already-complete on resume; no new work). |
| 2 | Done + one real bug fixed (CRLF digest false-mismatch on Windows checkouts, print crash on cp1252 console) — see commit `bfac2cad`. |
| 3 (US-SEQ-01) | Done through the stage-2 operator gate. Review loop caught 3 real defects in the strategy memo, forced a diff-proven v2, scoped re-review passed. **Held at the gate** — `apex-meta/orchestration/simulations/US-SEQ-01-20260712/06-gate-presentation.md` has a real, still-open operator decision (confirm/re-word the target, select a thesis A/B/C, supply pilot parameters). Commit `d2c6d5e7`. |
| Efficiency work | Done (Fixes 1+2+3 from a separate approved plan), **with one item still open** — see next section. Commit `1d8cfdf1`. |
| 4-8 | Not started. |

## ⚠️ The one thing to do first: confirm the doctrine-efficiency fix actually works

**Context:** Iteration 3 measured ~172K tokens for one story's stage 2. Investigation found the
4 populated agent doctrine domains (`meta-detective`, `knowledge-bank`, `informatics-design`,
`prompts-workflows`) were re-reading ~2,935 lines of mostly-ignorable migrated-v2 plumbing on every
single spawn. Fix: distilled `CORE.md` files (326 lines total) now exist for all four, and the
runtime `.claude/agents/<role>.md` files were updated to point at `CORE.md` first instead of the
full ESSENCE→BEST_PRACTICES→MISTAKES→TEMPLATES chain. A real root-cause bug was also found and fixed:
`apex-meta/orchestration/agents/DOCTRINE-MANIFEST.md`'s own central "read order" rule still said the
old chain and was silently overriding the new pointer (agents read that manifest for translation
rules and dutifully followed its stale read-order line too).

**The problem:** two live test re-spawns of `meta-detective` in the same session where these edits
were made — one before and one after the manifest fix — both still read the full doctrine chain and
never opened `CORE.md`, despite the on-disk files being confirmed correct both times. Most likely
explanation: Claude Code snapshots a custom subagent's system prompt when its type first becomes
available in a session, so mid-session edits don't take effect for spawns in that same session.

**What to do:** you are (presumably) in a fresh session now, which is exactly the condition needed to
test this. Before starting Iteration 4:

1. Spawn a `meta-detective` agent for the exact same task as the original test: re-run the US-SEQ-01
   stage-2 assumption-check against the **frozen v1 memo** (not v2 — the original, defect-containing
   version, so the result is comparable):
   - File: `apex-meta/orchestration/simulations/US-SEQ-01-20260712/01-strategy-options.md`
   - Frozen sha256 (LF-normalized): `886fb53276b5148d48be4467a61cf44d62035c368d4379c4c2bba40687477cb4`
   - Sources: the two files declared in that memo's `sources_evidence` (Hermes DB + ProcessRanking,
     under `apex-meta/kb/old-apex-full-orchestration-agent-kb-v2/OperatorResearch/AgnetFlows_Projects_Targets/`)
   - Full test prompt is preserved verbatim in this session's transcript (search for "A/B re-test v2
     after manifest fix" if you have transcript access) — or just ask the agent to do the same
     validity/assumption-check described in `Newplan.md`'s efficiency section.
2. Check the agent's own "files read" disclosure at the end of its response. **Pass condition:** it
   names `CORE.md` and does NOT separately read the full `ESSENCE.md`/`BEST_PRACTICES.md`/`MISTAKES.md`
   chain. **Fail condition:** it reads the full chain again, same as before.
3. Compare token usage (visible in the Agent tool's `<usage>` block) against the three baselines
   already on record: 82,839 / 84,271 / 77,744 tokens for this exact same task across the three prior
   attempts. A real fix should land meaningfully below that range.
4. **If it passes:** update `Newplan.md`'s "Doctrine efficiency work" section to mark verification
   closed, note the confirmed token count, and proceed to Iteration 4.
5. **If it still fails:** the caching hypothesis is wrong or there's a deeper issue — stop and
   investigate further (check whether `CORE.md`'s own content is somehow being skipped by the agent's
   own judgment, not a session-caching artifact) before spending Opus tokens on 5 more heavy story
   simulations at the un-fixed cost.

Do not skip this check and just proceed on faith — the whole point of the efficiency work was to stop
the five remaining stories from each costing ~172K+ tokens.

## Continuing the story simulations (Iterations 4-6)

Per `Newplan.md`'s "Model routing" table: stay on **Opus 4.8**, but per the "Doctrine efficiency work"
per-role effort split — not blanket high:
- Domain workers (media/script/offer-copy/etc.) and generative stages (strategy option drafting,
  applying routed corrections): **medium**.
- `meta-detective` review passes (first-pass and scoped re-review): **high** — do not cut this, it's
  where the real defect-catching happens.
- US-WORKSHOP-01's safety review and US-COMP-01's compliance review specifically: **xhigh**.

Order: US-MEDIA-01 → US-LEELA-01 → US-WORKSHOP-01 → US-OFFER-01 → US-COMP-01, one commit per story,
stop-and-report between each (per the stop-and-switch protocol in `Newplan.md` — it applies whenever
the *model* changes, which for 4-6 it mostly won't, but still report what was done and confirm before
continuing to the next story).

**US-SEQ-01's operator gate is a separate, still-open decision** (see the table above) — it does not
block Iterations 4-6 (each story is independent), but flag it to the operator early in case they want
to answer it in parallel.

## Key facts a fresh session won't have in context

- The user prefers **one branch only (`main`)** — this came up because they accidentally lost work to
  an auto-created git stash mid-session; recovered and merged everything into `main` deliberately.
  Don't create feature branches for this work without asking.
- The user is non-technical about git mechanics ("I have no idea what any of this means") — explain
  git actions in plain terms, don't assume familiarity.
- Committing requires no special permission in this repo/session (the user has been fine with commits
  proceeding as part of the iteration work), but **pushing to `origin/main`** has not been explicitly
  authorized yet — ask first.
- The user is cost-conscious about Opus token spend and appreciates being told plainly which model to
  select — always state the recommended model before starting work.
