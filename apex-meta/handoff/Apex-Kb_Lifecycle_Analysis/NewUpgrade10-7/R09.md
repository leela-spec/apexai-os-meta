research_report:  
id: R09  
verdict: ready_for_patch_building  
blocking_missing_inputs: []

target_files:  
- path: ".claude/skills/apex-kb/SKILL.md"  
reasons:  
- "R08 requires capability-first routing at the primary operational entrypoint."  
- "R06 requires the connector-readable quality checklist to be placed on the connector route, explicitly subordinate to terminal quality --strict."  
- "R07 requires the terminal route to identify the aggregate postflight command as the preferred deterministic completion interface."  
exact_symbols_or_sections:  
- "Immediately after `# Apex KB`"  
- "`## Operating contract`"  
- "`## Operator-facing v3 lifecycle and output tiers`"  
- "`## File navigation`"  
- "`## Capability precheck and truthful state cap`"  
- "`## Procedure`"  
- "`## Deterministic versus LLM ownership`"  
- "`## Completion gate`"  
ordered_changes:  
- "Insert `## Execution-surface router` immediately after the title and before the operating contract."  
- "Ask one routing question only: can the executor run repository Python commands in a live worktree and capture command, exit status, stdout, and stderr?"  
- "Route to `terminal_backed`, `connector_only`, or `unsupported`; command the executor to follow only the selected route."  
- "State that connector read-back proves stored content only and cannot prove lint, quality, retrieval freshness, postflight, or query readiness."  
- "Replace `Capability precheck and truthful state cap` with the router; remove the four independent capability fields."  
- "Replace the universal 12-step procedure with `Connector-only semantic compile` and `Terminal-backed lifecycle`."  
- "In the connector route, allow only complete new semantic files and complete rewrites of explicitly owned semantic files."  
- "Prohibit partial edits, appends, `wiki/index.md` mutation, manifest/hash mutation, machine-generated sections, derived indexes, and deterministic certification."  
- "Require one page per context window and the sequence: bounded source read, complete authoring, complete reread, mirrored precheck, whole-file write, stored-content readback, manifest entry."  
- "Embed the R06 checklist once in the connector route, using all eleven runtime reason-code concepts and stating that only `quality --strict` certifies compliance."  
- "Require `log/connector-compile-<run-id>.json` as a complete new whole-file handoff artifact with the fields established by R08."  
- "Cap connector completion at `compiled_unvalidated`."  
- "Define the terminal route by reference to the command contract, retrieval contract, acceptance tests, and example runbook rather than duplicating command sequences."  
- "Identify `postflight` as the preferred bounded deterministic completion command after semantic writes."  
- "Retain `partial` and `query_ready`; do not introduce additional lifecycle states."  
- "Relabel file-navigation entries for the state machine and runbook as deprecated reference and terminal-backed example respectively."  
- "Add one sentence to the ownership section stating that connector execution is restricted further to complete semantic artifacts and cannot perform Python-owned mutation."  
preserve_unchanged:  
- "Package name, data-root contract, canonical/derived path lists, and no-mutation boundary."  
- "Continuous Phase 1-to-Phase 2 policy and its existing safe stop modes."  
- "Output-tier names."  
- "Deterministic-versus-LLM ownership categories except for the connector restriction sentence."  
- "Existing semantic acceptance requirement."  
- "Existing truthful completion labels: compiled_unvalidated, partial, query_ready."  
- "No command syntax duplication inside the connector route."  
dependencies:  
- "The checklist wording must match the runtime reason-code registry and acceptance fixtures changed in the same patch set."  
- "The terminal route may name `postflight` only when the runtime and command-contract patches are included cumulatively."  
- "Navigation wording depends on the state-machine, runbook, and package-manifest authority changes."  
acceptance_checks:  
- "The execution-surface router appears before all command/navigation material."  
- "A connector-only executor is not instructed to read or execute terminal-only procedures."  
- "The connector route contains all R06 checks, terminal-authority language, and no claim of equivalence certification."  
- "The connector route permits only create-complete-file and complete-rewrite operations."  
- "The connector compile manifest has the fixed values `execution_surface: connector_only`, `deterministic_commands_run: []`, and `maximum_completion_state: compiled_unvalidated`."  
- "The terminal route references, but does not duplicate, the deterministic contracts."  
- "`query_ready` still requires deterministic postflight, fresh retrieval, semantic acceptance, and retained evidence."  
patch_file_name: "001-apex-kb-skill-capability-routing.patch"

```
- path: "apex-meta/scripts/apex_kb.py"
  reasons:
    - "R07 accepts one thin aggregate `postflight` command."
    - "R06 requires a single runtime reason-code authority and fixture drift protection."
  exact_symbols_or_sections:
    - "Quality reason-code constants or registry near existing quality constants"
    - "`normalize_global_flag_placement`"
    - "`cmd_index`"
    - "`cmd_lint`"
    - "`cmd_quality`"
    - "`cmd_audit`"
    - "`cmd_status`"
    - "New `cmd_postflight` and only minimal local helper functions needed to normalize stage evidence"
    - "`build_parser`"
    - "`main` exit handling"
  ordered_changes:
    - "Define the eleven quality reason-code strings once in a named immutable registry and replace duplicated literal production sites with registry references without changing thresholds or detection behavior."
    - "Add `postflight` to the command set accepted by global-flag normalization."
    - "Implement `cmd_postflight(args)` as a fixed-stage aggregate that calls existing Python functions directly and never shells out."
    - "Use this exact order: wiki index, retrieval build, lint strict, quality strict, audit, status, retrieval stale."
    - "Import `apex_kb_retrieval` using the existing internal-module delegation pattern already used by `cmd_query`."
    - "Pass one resolved KB root and the same effective dry-run/write mode to every stage."
    - "Force strict behavior only on lint and quality delegates."
    - "Preserve each delegate's complete result under `steps[].result`."
    - "Record stage, delegate, status, blocking flag, dependency status, and skipped-stage reason."
    - "On wiki-index failure, skip retrieval build and retrieval stale as acceptance proof, while continuing safe diagnostic stages."
    - "On retrieval-build failure, skip retrieval stale as acceptance proof, while continuing lint, quality, audit, and status."
    - "Continue diagnostic stages after lint or quality failure."
    - "Treat audit findings as report-only."
    - "In default dry-run, resolve and execute delegate previews/read-only checks, return `status: planned`, `evidence_complete: false`, and never infer freshness from planned writes."
    - "With `--allow-write`, return pass only when all blocking stages pass and final retrieval stale status is fresh."
    - "Emit schema `apex.kb.postflight.v1` with the complete R07 field set."
    - "Add a `postflight` subparser with no new lifecycle, retry, graph, scheduling, semantic-review, or ranking options."
    - "Map postflight planned/pass to exit 0, fail to exit 2, and error to exit 1 while leaving individual-command behavior unchanged."
  preserve_unchanged:
    - "All existing delegate implementations and their output objects."
    - "Quality thresholds, page-type exceptions, parsing regexes, and repair-reason semantics."
    - "Retrieval ranking and index construction."
    - "Existing dry-run and `--allow-write` policy."
    - "Existing direct individual commands and aliases."
    - "Existing output-json path safety."
    - "No subprocess or shell invocation."
    - "No persisted orchestration ledger, retries, dynamic stage graph, rollback engine, or semantic review."
  dependencies:
    - "Runtime reason-code registry must remain synchronized with SKILL.md checklist and acceptance fixtures."
    - "Command documentation and runbook must describe only the implemented stage order and evidence limits."
    - "The retrieval module remains unchanged and is called through its existing public command functions."
  acceptance_checks:
    - "Default postflight writes nothing, returns planned, evidence_complete false, and exit 0."
    - "`--allow-write --dry-run` behaves identically to dry-run."
    - "`--allow-write` rebuilds wiki and retrieval indexes before final status/stale acceptance."
    - "Every stage receives the same resolved KB root."
    - "Quality repair candidates produce overall fail and exit 2 while preserving exact reasons."
    - "Strict lint failure produces overall fail and exit 2."
    - "A retrieval-build exception cannot become a fresh/pass result."
    - "Missing or unreadable retrieval metadata cannot produce pass."
    - "Audit items alone do not fail postflight."
    - "An internal stage exception yields status error, retained prior evidence, skipped dependent stages, and exit 1."
    - "No write can escape the KB root."
    - "All existing direct commands retain current behavior."
    - "A registry consistency fixture proves every documented checklist code exists in runtime and every runtime code is documented."
  patch_file_name: "002-apex-kb-postflight-runtime.patch"

- path: ".claude/skills/apex-kb/references/script-command-contract.md"
  reasons:
    - "R07 requires one bounded command contract for the implemented aggregator."
    - "R06 permits command documentation for the checklist authority and strict verification limit."
  exact_symbols_or_sections:
    - "`## apex_kb.py commands` command table"
    - "`## Exit policy`"
    - "`## Postflight evidence contract`"
  ordered_changes:
    - "Add `postflight` to the lifecycle command table as a deterministic aggregate with possible index/retrieval writes only under existing write policy."
    - "Replace the current generic postflight paragraph with a bounded `postflight` command contract."
    - "Document the exact seven-stage order and identify audit as nonblocking."
    - "Document default dry-run as `planned`, not pass."
    - "Document `--allow-write`, `--dry-run` override, dependency skips, and diagnostic continuation."
    - "Document schema `apex.kb.postflight.v1`, full delegate-result preservation, final assertions, and exit codes."
    - "State explicitly that the command does not run semantic review, query-eval, graph, source intake, Phase 1/2 drafting, commit, or push."
    - "State explicitly that it delegates existing implementations and must not duplicate validation logic."
    - "State that the SKILL.md connector checklist is a mirrored precheck and terminal `quality --strict` remains authoritative."
  preserve_unchanged:
    - "Shared network, dependency, shell-out, path-safety, and write-policy rules."
    - "Existing individual command contracts."
    - "Retrieval command definitions."
    - "Quality reason semantics and thresholds."
    - "No broader lifecycle rewrite."
  dependencies:
    - "Must be patched after or cumulatively with the implemented runtime command."
    - "Command stage names and output fields must match `cmd_postflight` exactly."
  acceptance_checks:
    - "Every documented postflight stage has a matching runtime delegate."
    - "Dry-run is never described as successful certification."
    - "The documented exit policy matches runtime."
    - "No unsupported semantic, retrieval-ranking, retry, or workflow behavior is claimed."
    - "Direct individual commands remain documented and supported."
  patch_file_name: "003-apex-kb-postflight-command-contract.patch"

- path: ".claude/skills/apex-kb/references/acceptance-tests.md"
  reasons:
    - "R06 requires checklist-equivalence and drift fixtures."
    - "R07 requires aggregate-postflight behavior fixtures."
  exact_symbols_or_sections:
    - "`## Phase 2 thin-but-structurally-complete regression fixtures`"
    - "New `## Connector checklist equivalence` section"
    - "New `## Aggregate postflight` section"
  ordered_changes:
    - "Replace the prose-only Phase 2 fixture instruction with executable fixture definitions or a deterministic fixture script invocation covering every runtime reason code."
    - "Include one passing multi-source concept and the exact failing-page matrix from R06."
    - "Include the valid narrow-entity exception."
    - "Assert that every checklist reason code exists in the runtime registry and every registry code is represented by documentation and at least one triggering fixture."
    - "Assert that changes to headings, section-word floor, claim/route/pointer counting, or page-type exceptions require fixture updates in the same patch."
    - "Add postflight dry-run, allow-write, allow-write-plus-dry-run, strict quality failure, strict lint failure, retrieval-build failure, missing/unreadable metadata, shared KB-root, path safety, and direct-command-regression fixtures."
    - "Validate the complete postflight JSON envelope and stage order."
    - "Validate that planned writes cannot produce evidence_complete true or fresh/pass assertions."
  preserve_unchanged:
    - "Existing source-intake, Phase 0, retrieval, query-eval, graph, and payload-manifest fixtures."
    - "No LLM grading or subjective prose-quality assertions."
    - "No universal word threshold."
    - "No external dependencies or network access."
  dependencies:
    - "Fixture reason codes must derive from the runtime registry."
    - "Postflight fixtures depend on the implemented command and documented schema."
    - "SKILL.md checklist wording must be validated against the same registry."
  acceptance_checks:
    - "One positive fixture passes quality strict."
    - "Each of the eleven reason codes has at least one deterministic triggering fixture."
    - "The narrow entity fixture remains exempt from thin-layer and single-claim failure solely due to concision."
    - "Postflight fixtures prove exact stage order, dependency skipping, result preservation, overall status, and exit code."
    - "No fixture treats connector readback as deterministic execution proof."
  patch_file_name: "004-apex-kb-acceptance-fixtures.patch"

- path: ".claude/skills/apex-kb/package-manifest.md"
  reasons:
    - "R08 requires inventory authority alignment."
  exact_symbols_or_sections:
    - "`## Inventory` rows for `references/lifecycle-state-machine.md` and `examples/lifecycle-runbook.md`"
    - "`## Lifecycle authority`"
  ordered_changes:
    - "Change the state-machine role to `Deprecated historical/reference state model; SKILL.md remains operational authority`."
    - "Change the runbook role to `Example-only terminal-backed lifecycle sequence`."
    - "Leave the existing lifecycle-authority clause intact."
  preserve_unchanged:
    - "Package identity, script paths, inventory membership, canonical/derived path lists, executability note, and scope exclusions."
    - "Do not add patch-plan or compile-manifest files to runtime inventory."
  dependencies:
    - "Role descriptions must match the banners added to the referenced files and the SKILL.md navigation labels."
  acceptance_checks:
    - "No inventory row presents the state machine or runbook as co-authoritative."
    - "`SKILL.md` remains explicitly identified as the sole operational lifecycle authority."
  patch_file_name: "005-apex-kb-package-authority-alignment.patch"

- path: ".claude/skills/apex-kb/references/lifecycle-state-machine.md"
  reasons:
    - "R08 requires an in-file deprecation banner because the current file still reads as executable authority."
  exact_symbols_or_sections:
    - "Immediately after `# Apex KB Lifecycle State Machine`"
  ordered_changes:
    - "Insert the exact R08 deprecation banner stating that SKILL.md is the sole operational lifecycle authority and this file is historical/reference only."
    - "Make no state, transition, command, or completion-model edits."
  preserve_unchanged:
    - "All historical states, transitions, command examples, and macro-state content."
  dependencies:
    - "Banner wording must align with the package-manifest role and SKILL.md navigation description."
  acceptance_checks:
    - "The first substantive content after the title is the deprecation banner."
    - "The file cannot reasonably be interpreted as independent procedure authority."
    - "No lifecycle redesign is introduced."
  patch_file_name: "006-apex-kb-state-machine-deprecation-banner.patch"

- path: ".claude/skills/apex-kb/examples/lifecycle-runbook.md"
  reasons:
    - "R08 requires an example-only terminal-backed banner."
    - "R07 requires the operator example to use the aggregate postflight command rather than a manually sequenced primary interface."
  exact_symbols_or_sections:
    - "Immediately after `# Apex KB Lifecycle Runbook`"
    - "`## C. Postflight`"
  ordered_changes:
    - "Insert the exact R08 example-only banner directing executors to the SKILL.md execution-surface router first."
    - "Replace the manual postflight sentence with one primary `postflight` invocation description."
    - "State that default invocation is preview/planned and `--allow-write` is required to rebuild derived artifacts and produce certifying deterministic evidence."
    - "Retain individual commands as diagnostic or maintenance interfaces, not the preferred completion sequence."
    - "State that the runbook applies only to terminal-backed execution."
  preserve_unchanged:
    - "Prepare, ingest/compile, and query/maintain examples except for authority clarification."
    - "Output tiers and KB-root boundary."
    - "No connector procedure duplication."
  dependencies:
    - "The postflight example depends on the implemented runtime command and command contract."
    - "Banner wording must match SKILL.md and package-manifest authority descriptions."
  acceptance_checks:
    - "The runbook is visibly example-only before any procedure text."
    - "Connector-only executors are explicitly excluded."
    - "Postflight dry-run is not described as pass."
    - "No separate workflow engine or alternative lifecycle is introduced."
  patch_file_name: "007-apex-kb-runbook-banner-and-postflight.patch"
```

excluded_files:  
- path: ".claude/skills/apex-kb/references/ingest-query-lint-audit-rules.md"  
reason: "No change. The existing file already defines deterministic quality as structural, terminal-owned, and followed by bounded semantic review. The connector precheck belongs once in SKILL.md, not as a second checklist copy."  
- path: ".claude/skills/apex-kb/references/kb-contract.md"  
reason: "No change. R06 left checklist placement open, but the orchestrator and R08 selected SKILL.md as the connector copy. Editing the page contract would duplicate authority and reopen the explicitly gated consolidation wave."  
- path: ".claude/skills/apex-kb/templates/wiki-page-templates.md"  
reason: "No change. Duplicating the full checklist here would create drift. Template consolidation remains a second-wave operator decision."  
- path: "apex-meta/scripts/apex_kb_retrieval.py"  
reason: "No change. Postflight must call existing retrieval delegates directly; retrieval implementation and ranking are explicitly out of scope."  
- path: ".claude/skills/apex-kb/references/retrieval-contract.md"  
reason: "No change. The existing retrieval build and stale contract remains authoritative and is only referenced by the terminal route."  
- path: "Any existing KB wiki, ingest-analysis, source manifest, derived index, query output, or audit file"  
reason: "No runtime KB content mutation belongs in this plan."  
- path: "Any new routing, lifecycle, checklist, or postflight document"  
reason: "The accepted design modifies existing authorities only."

patch_build_order:  
- "1. Patch `apex-meta/scripts/apex_kb.py`: reason-code registry and aggregate postflight."  
- "2. Patch `references/script-command-contract.md` against the implemented runtime."  
- "3. Patch `references/acceptance-tests.md` with runtime-linked checklist and postflight fixtures."  
- "4. Patch `SKILL.md` with the capability router, connector route/checklist, compile manifest, and terminal route."  
- "5. Patch `references/lifecycle-state-machine.md` with the deprecation banner."  
- "6. Patch `examples/lifecycle-runbook.md` with the example-only banner and postflight usage."  
- "7. Patch `package-manifest.md` to align inventory authority descriptions."

cumulative_validation_order:  
- "Validate each generated patch independently with `git apply --check` against the same current-main baseline."  
- "Validate cumulative application in patch-build order."  
- "Compile `apex-meta/scripts/apex_kb.py` and `apex-meta/scripts/apex_kb_retrieval.py`."  
- "Run lifecycle helper `--help`; confirm `postflight` appears and all existing commands remain."  
- "Run direct-command regression checks before postflight fixtures."  
- "Run reason-code registry/checklist/fixture synchronization checks."  
- "Run all R06 quality fixtures."  
- "Run all R07 postflight dry-run, write-mode, failure, dependency, schema, and exit-code fixtures."  
- "Run documentation/runtime alignment checks."  
- "Inspect cumulative diff and confirm exactly seven target files."

terminal_apply_and_validation_order:  
- "1. Checkout and fast-forward `main`."  
- "2. Confirm the target baseline is current and the seven target files match the patch-builder baseline."  
- "3. Run `git apply --check` for every patch individually and cumulatively."  
- "4. Apply all patches in the declared order."  
- "5. Run Python compile checks for both scripts."  
- "6. Run help and parser checks, including global flags before and after `postflight`."  
- "7. Run existing direct-command smoke tests."  
- "8. Run the complete quality reason-code fixture matrix."  
- "9. Run postflight default dry-run and verify planned/no-write evidence."  
- "10. Run postflight with `--allow-write --dry-run` and verify identical no-write behavior."  
- "11. Run postflight with `--allow-write` on a passing fixture and verify stage order, fresh retrieval, pass status, evidence_complete true, and exit 0."  
- "12. Run strict quality and lint failure fixtures and verify fail status with exit 2."  
- "13. Run retrieval-build exception and unreadable/missing metadata fixtures and verify no false pass."  
- "14. Validate connector-route text, compile-manifest schema, state cap, banners, and authority descriptions."  
- "15. Confirm changed-file scope is exactly the seven target files."  
- "16. Commit and push `main` only after every check passes."

drift_rejected:  
- "Quality-threshold, word-floor, reason-trigger, pointer-granularity, or page-profile redesign."  
- "Retrieval ranking, chunking, backend, metadata-schema, or stale-algorithm redesign."  
- "General CLI cleanup, alias removal, flag-normalization redesign, or unrelated parser changes."  
- "Adding query-eval, graph, semantic review, source intake, Phase 1, Phase 2 drafting, commit, or push to postflight."  
- "New lifecycle states such as deterministically_validated or repair_required."  
- "New agents, roles, workflow engines, schedulers, retries, rollback managers, or persisted orchestration state."  
- "Broad documentation cleanup or page-value-contract consolidation."  
- "A generated connector checklist."  
- "Checklist duplication in the KB contract, templates, and operational rules."  
- "Any connector permission to partially edit existing files, append to wiki/index.md, mutate manifests/hashes, or rebuild derived artifacts."  
- "Making audit findings blocking without an existing severity contract."  
- "Treating connector readback, headings, file existence, or planned writes as deterministic proof."  
- "Changing direct individual command behavior merely to support aggregation."  
- "Modifying any KB corpus or unrelated Apex Plan, Sync, Session, PreCap, FlowRecap, APSU, or personal orchestration state."

unresolved_questions_that_block_patch_building: []