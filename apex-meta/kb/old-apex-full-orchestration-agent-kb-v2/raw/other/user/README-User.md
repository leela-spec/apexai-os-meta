# User Area

This area holds user-owned runtime files and personal operating context.

- `AGENTS.md` contains user override rules.
- `TOOLS.md` contains local tool notes.
- `identity/` contains mission, identity, and human-profile files.
- `memory/` contains the segmented memory system.

User-owned files should stay small, durable, and easy to maintain.
