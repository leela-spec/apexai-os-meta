# Best Practice: How We Created Git-Executable Patch Files Reliably

## 1. Core principle

The successful pattern was:

```text
Do not ask AI to “edit files.”
Ask AI to create patch artifacts against exact live file contents,
then validate those artifacts deterministically before Codex applies them.
```

The key improvement was shifting from **AI-generated conceptual diffs** to **Git-native, blob-anchored patch artifacts**.

---

# 2. What made this patch pack work

## A. We read the live target files first

Before generating patches, the process fetched the actual current files from the repo:

```text
.claude/skills/apex-kb/templates/wiki-page-templates.md
.claude/skills/apex-kb/templates/ingest-analysis-template.md
.claude/skills/apex-kb/references/kb-contract.md
.claude/skills/apex-kb/references/ingest-query-lint-audit-rules.md
.claude/skills/apex-kb/references/acceptance-tests.md
.claude/skills/apex-kb/SKILL.md
.claude/skills/apex-kb/references/lifecycle-state-machine.md
.claude/skills/apex-kb/references/knowledge-promotion-rules.md
.claude/skills/apex-kb/templates/kb-schema-template.md
```

This avoided the usual failure mode:

```text
AI writes a patch against an imagined file shape.
```

Instead, the patches were generated against the actual current content.

---

## B. One patch file per target file

This was critical.

Bad pattern:

```text
one giant patch touching many files
```

Successful pattern:

```text
001-wiki-page-templates.patch       -> one target file
002-ingest-analysis-template.patch  -> one target file
003-kb-contract.patch               -> one target file
...
009-kb-schema-template.patch        -> one target file
```

This made every patch:

- easier to validate;
    
- easier to diagnose;
    
- easier to re-run;
    
- easier to isolate if one file failed;
    
- safer for Codex to execute.
    

---

## C. Patch files were generated by Git, not hand-written

The reliable method was effectively:

```bash
# 1. Start from exact baseline file
# 2. Apply intended edit locally
# 3. Let git generate the diff
git diff --no-ext-diff -- <target-file> > <numbered-patch-file>
```

That matters because Git writes proper unified diff structure:

```text
diff --git a/path b/path
index <old-blob>..<new-blob>
--- a/path
+++ b/path
@@ ...
```

The `index <old>..<new>` line was the breakthrough. Codex later verified:

```text
old_blob checks: all MATCH
```

That means the patch was anchored to the exact file version in the repo.

---

## D. Every patch was checked before export

Each patch had to pass:

```bash
git apply --check <patch>
```

Then the full sequence had to pass cumulatively:

```bash
for p in *.patch; do git apply --check "$p"; done
for p in *.patch; do git apply "$p"; done
```

This prevented the usual problem:

```text
Patch looks plausible in Markdown but is not actually appliable by Git.
```

---

## E. Target files were reverted after patch creation

The builder did **not** leave the real target files modified.

Correct state after patch-pack creation:

```text
repo contains patch artifacts only
target files are clean/unmodified
```

This made the patch pack a portable artifact for Codex.

---

## F. A manifest mapped every patch to its target

The manifest explicitly recorded:

```yaml
patch_to_target_map:
  "001-wiki-page-templates.patch": ".claude/skills/apex-kb/templates/wiki-page-templates.md"
  "002-ingest-analysis-template.patch": ".claude/skills/apex-kb/templates/ingest-analysis-template.md"
  ...
```

This allowed the applier script to reject any patch that touched the wrong file.

---

## G. The applier verified semantic markers after applying

The process did not stop at “patch applied.”

It also checked for required outcome markers:

```text
Adaptive Ranked Source Set
Macro / Meso / Micro
Routes Here
Uncertainty / Raw Source Triggers
```

And forbidden markers:

```text
page_value_score
source_cluster_map
```

This is important because a patch can apply cleanly and still produce the wrong conceptual result.

---

## H. Scope verification was mandatory

The applier checked that only the intended nine files changed.

This prevented silent scope creep into:

```text
apex-meta/scripts/
apex-meta/kb/*/wiki/
apex-meta/kb/*/ingest-analysis/
.claude/skills/apex-kb2/
raw/
sources/
derived/
outputs/
```

That was a major safety layer.

---

# 3. The reusable future process

Use this workflow for future Apex patch packs.

## Step 1 — Lock the intended change

Create a short source-grounded change plan:

```yaml
change_plan:
  target_files:
    - "<exact file path>"
  non_goals:
    - "do not patch scripts"
    - "do not patch runtime KB content"
    - "do not touch adjacent package"
  required_markers:
    - "<string that must exist after patch>"
  forbidden_markers:
    - "<string that must not exist after patch>"
```

## Step 2 — Read live repo files

Never generate patches from memory.

Fetch/read the exact target files from the live branch first.

```text
repo: leela-spec/apexai-os-meta
branch: main
```

## Step 3 — Create a temporary patch workspace

Use a real Git worktree, even if it is a synthetic local one.

Baseline files must exactly match the live repo.

## Step 4 — Edit one target file at a time

For each target file:

```text
edit file
git diff -- <target-file> > 00N-name.patch
git checkout -- <target-file>
git apply --check 00N-name.patch
```

Do not generate multi-file patch blobs.

## Step 5 — Validate cumulatively

After all patches exist:

```bash
for p in *.patch; do git apply --check "$p"; done
for p in *.patch; do git apply "$p"; done
git diff --name-only
```

Expected changed files must equal the target list exactly.

## Step 6 — Run semantic marker checks

Check:

```bash
grep -R "Required Marker" <package-root>
grep -R "Forbidden Marker" <package-root>
```

A patch pack is not valid just because `git apply` works.

It must also prove it produced the intended contract.

## Step 7 — Export patch pack, manifest, and deterministic applier

A complete patch pack should contain:

```text
000-patch-manifest.md
001-*.patch
002-*.patch
...
999-codex-apply-handoff.md
apply_patch_pack.py or .ps1
```

## Step 8 — Codex only runs the script

Codex should not manually patch unless every deterministic path fails.

Good Codex role:

```text
Run script.
Report result.
Do not improvise.
```

Bad Codex role:

```text
Read diff.
Think about how to edit.
Manually modify files.
```

---

# 4. Required safety checks for future patch packs

Every future applier should verify:

```yaml
safety_checks:
  repo_origin: "expected repo"
  branch: "expected branch"
  worktree_clean_before_start: true
  old_blob_matches_live_file: true
  one_patch_per_target_file: true
  git_apply_check_passes: true
  changed_files_exactly_expected: true
  forbidden_paths_unchanged: true
  required_markers_present: true
  forbidden_markers_absent: true
  final_git_status_clean: true
```

The most important ones are:

```text
old_blob_matches_live_file
git apply --check
changed_files_exactly_expected
semantic marker checks
```

---

# 5. Why this worked this time

This patch pack worked because it had the ingredients previous patch attempts lacked:

```yaml
why_it_worked:
  exact_live_file_basis: true
  git_generated_unified_diffs: true
  one_patch_per_file: true
  old_blob_anchoring: true
  patch_target_manifest: true
  deterministic_apply_script: true
  dirty_tree_guard: true
  scope_verification: true
  semantic_marker_verification: true
  fallback_logic_available: true
```

The decisive factor was not the fallback logic. The decisive factor was that the fallback logic was **not needed** because the patches were created cleanly in the first place.

Codex verification confirms the target state:

```yaml
codex_verification:
  apply_mode: plain_git_apply
  old_blob_checks: all_MATCH
  fallback_needed: false
  manual_repair_needed: false
  scope_violation: false
  push_succeeded: true
```

---

# 6. Short reusable rule

For future runs:

```text
Patch packs must be generated from exact live target files,
one target file per patch,
with Git-generated diffs,
old-blob validation,
git apply --check,
exact changed-file scope verification,
and semantic marker checks.
```

That is the working pattern.