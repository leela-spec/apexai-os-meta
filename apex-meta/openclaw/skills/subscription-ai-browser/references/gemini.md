# Gemini

1. Require exactly one shared tab at `gemini.google.com`.
2. Apply the exact declared session, mode, model, and reasoning settings and verify them from a fresh snapshot.
3. Insert and verify the complete prompt before one submission.
4. For Deep Research, click `Start research` only after Gemini presents its plan; do not alter the plan.
5. Long research is resumable at the same conversation URL. Do not start a duplicate run after navigation or delay.
6. Wait for completion and capture the final response verbatim.
