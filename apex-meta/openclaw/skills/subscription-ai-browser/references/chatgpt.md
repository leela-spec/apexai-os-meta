# ChatGPT

1. Require exactly one shared tab at `chatgpt.com`.
2. Apply the exact declared session, mode, model, and reasoning settings and verify them from a fresh snapshot.
3. The composer sends on bare Enter. Insert the exact prompt in one operation, read it back, then submit once.
4. If a keyboard dispatch times out, inspect the page before retrying.
5. Wait for generation to finish and capture the final response verbatim.
