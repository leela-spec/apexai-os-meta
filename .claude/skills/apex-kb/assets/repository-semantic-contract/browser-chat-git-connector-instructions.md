# Browser Chat with Git Connector Instructions

Work one topic at a time. Read all repository-local semantic-contract files, then the topic registry entry, Phase 0 rankings, and existing topic ledger. Lock target questions first. Use rankings only to locate candidates; read canonical sources and update the ledger after each source. Never represent an unopened source as evidence.

Write and reread complete Phase 1 analyses. Dispose every concept/entity candidate explicitly. Choose summary/concept/entity topology by recurring retrieval value. Write pages that directly answer declared questions, then reread them. A boundary notice or reopen trigger is not an answer when the source is readable.

Use a fresh browser-chat context for page-only query evaluation and claim-entailment review. Record the acceptance artifact. Do not self-certify, count files/headings as success, run deterministic commands, or claim `query_ready`. If context or connector reliability prevents completion, update the ledger and report `partial`.
