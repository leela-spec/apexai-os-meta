# ranking-and-validation-rules.md

```
# Ranking and Validation Rules```yamlrules_metadata:  artifact_scope: current_project_status_overview  schema_version: "0.1"  purpose: ranking_validation_and_failure_rules  hierarchy:    - project    - task    - subtask  rating_format: "[priority/urgency/date]"
```

# Rating Parser

```
rating_parser:  syntax: "[priority/urgency/date]"  valid_pattern:    plain_text_description: "A bracketed triplet with two integers and one date-or-NA field."    examples:      - "[100/95/NA]"      - "[77/30/14-08]"      - "[1/1/NA]"  fields:    priority:      position: 1      type: integer      minimum: 1      maximum: 100      meaning: strategic_importance    urgency:      position: 2      type: integer      minimum: 1      maximum: 100      meaning: time_pressure    date:      position: 3      type: string      allowed_values:        - "DD-MM"        - "NA"      meaning: real_deadline_if_known  invalid_examples:    - "[high/low/NA]"    - "[101/50/NA]"    - "[80/0/NA]"    - "[80/50/2026-08-14]"    - "80/50/NA"    - "[80/50]"
```

# Ranking Rule

```
ranking_rule:  name: manual_override_then_deadline_first_priority_second_urgency_third  order:    1: manual_override_or_pinned_order_if_present    2: deadline_first    3: priority_second    4: urgency_third  manual_override:    beats_automatic_ranking: true    allowed_actions:      pin: "Force task into operator-defined top position."      promote: "Move task higher than automatic ranking would place it."      demote: "Move task lower than automatic ranking would place it."      freeze: "Keep task in its current rank until operator changes it."  automatic_ranking:    applies_when: "No manual override affects the compared tasks."    compare_sequence:      - compare_deadline_pressure      - compare_priority      - compare_urgency  tie_breaking:    first: "higher priority"    second: "higher urgency"    third: "existing order in project section"    fourth: "operator_review_needed"
```

# Deadline Pressure

```
deadline_pressure:  purpose: "Convert the date field into ranking pressure without turning the overview into a calendar planner."  assumptions:    date_format: "DD-MM"    active_year: "current planning year unless operator supplies another year"    na_deadline: "No fixed date known."  rules:    overdue:      pressure: highest      ranking_effect: "Rises above future dated tasks unless manual override says otherwise."    due_today:      pressure: highest      ranking_effect: "Rises near top unless manual override says otherwise."    near_deadline:      pressure: high      suggested_window: "1-7 days"      ranking_effect: "Real date beats NA when soon."    medium_deadline:      pressure: medium      suggested_window: "8-30 days"      ranking_effect: "Can outrank NA when priority is comparable."    distant_deadline:      pressure: low      suggested_window: "more than 30 days"      ranking_effect: "Deadline exists but should not dominate high-priority NA tasks automatically."    no_deadline:      value: "NA"      pressure: none      ranking_effect: "Does not mean unimportant."  important_rule:    - "A real soon date beats NA."    - "Overdue or near-deadline tasks rise highest."    - "NA means no known fixed deadline, not low priority."
```

# Validation Checks

```
validation_checks:  rating_checks:    every_task_has_rating:      required: true      rule: "Every task must include [priority/urgency/date]."    every_subtask_has_rating:      required: true      rule: "Every subtask must include [priority/urgency/date]."    rating_pattern_valid:      required: true      rule: "Every rating must match [number/number/date-or-NA]."    priority_range_valid:      required: true      rule: "Priority must be an integer from 1 to 100."    urgency_range_valid:      required: true      rule: "Urgency must be an integer from 1 to 100."    date_valid:      required: true      rule: "Date must be DD-MM or NA."  placement_checks:    every_task_belongs_somewhere:      required: true      rule: "Every task must belong to a project or to unassigned."    every_subtask_belongs_to_task:      required: true      rule: "Every subtask must sit under exactly one task."    unassigned_removed_after_assignment:      required: true      rule: "Once an item is assigned to a project, remove it from unassigned."    no_duplicate_after_assignment:      required: true      rule: "Do not keep the same item in unassigned and project sections."  structure_checks:    project_task_subtask_only:      required: true      rule: "Use only project, task, and subtask as structural layers."    no_workstream_section:      required: true      rule: "Do not create a workstream section or workstream layer."    no_project_ids_required:      required: true      rule: "Human-facing project sections use project name only."    no_heavy_task_ids_required:      required: true      rule: "Use compact labels, not heavy ID systems."    no_detailed_project_state_expansion:      required: true      rule: "Do not expand into detailed project history, full decision records, artifact registries, or execution logs."  blocker_checks:    blockers_only_when_present:      required: true      rule: "Blockers may appear only when the source context names a real blocker."    no_invented_blockers:      required: true      rule: "Do not infer or invent blockers from vague uncertainty."  ranking_checks:    manual_override_preserved:      required: true      rule: "Pinned, promoted, demoted, or frozen tasks must keep operator override."    deadline_first_applied:      required: true      rule: "When no manual override applies, deadline pressure is compared first."    priority_second_applied:      required: true      rule: "After deadline pressure, compare priority."    urgency_third_applied:      required: true      rule: "After priority, compare urgency."    ranking_conflict_flagged:      required: true      rule: "If automatic ranking conflicts with manual override, preserve override and flag the conflict."
```

# Failure Modes

```
failure_modes:  missing_metrics:    trigger:      - task_has_no_rating      - subtask_has_no_rating      - rating_contains_unknown_value    correction:      - "Add an estimated rating only when source context supports it."      - "Otherwise mark operator-review-needed."      - "Do not remove the item solely because metrics are missing."  invalid_date:    trigger:      - date_not_DD_MM      - date_not_NA      - impossible_calendar_date    correction:      - "Replace with NA if no reliable date exists."      - "Mark operator-review-needed."      - "Preserve original date in a short review note only if useful."  over_detailed_project_expansion:    trigger:      - detailed_project_history_added      - decision_registry_added      - artifact_registry_added      - execution_log_added      - full_status_merge_behavior_added    correction:      - "Compress back to project → task → subtask."      - "Move detailed material out of the overview or summarize as a task/subtask."      - "Mark source context as too detailed if needed."  unassigned_duplicate_after_assignment:    trigger:      - same_item_appears_in_unassigned_and_project_section      - item_assigned_but_unassigned_copy_remains    correction:      - "Keep the project-assigned version."      - "Remove the unassigned version."      - "Add operator-review-needed only if assignment is uncertain."  ranking_conflict_between_deadline_and_manual_override:    trigger:      - manual_override_places_task_against_deadline_first_order      - pinned_task_has_lower_deadline_pressure_than_auto_top_task    correction:      - "Preserve operator override."      - "Flag ranking_conflict for operator review."      - "Do not silently reorder overridden tasks."  invented_blocker:    trigger:      - blocker_added_without_source_context      - uncertainty_rewritten_as_blocker    correction:      - "Remove blocker."      - "Use operator-review-needed if uncertainty matters."  workstream_layer_created:    trigger:      - workstream_heading_present      - task_group_named_as_workstream    correction:      - "Remove workstream layer."      - "Promote or compress its children into project tasks."
```

# Correction Behavior

```
correction_behavior:  preserve_operator_override:    required: true    applies_to:      - pinned_tasks      - promoted_tasks      - demoted_tasks      - frozen_tasks    rule: "Manual override beats automatic ranking."  ask_for_review_only_when_needed:    required: true    ask_when:      - missing_rating_cannot_be_estimated      - date_is_invalid_or_ambiguous      - assignment_is_uncertain      - duplicate_item_cannot_be_resolved      - manual_override_conflicts_with_deadline_pressure      - blocker_status_is_unclear    do_not_ask_when:      - correction_is_mechanical      - item_can_be safely_removed_from_unassigned_after_assignment      - invalid_structure_can_be_compacted_without losing meaning      - NA_deadline_is_valid  mark_uncertain_ratings:    required: true    marker: operator-review-needed    applies_to:      - invented_starter_ratings      - weakly_supported_priority      - weakly_supported_urgency      - unknown_deadline      - ambiguous_task_assignment  preserve_compactness:    required: true    rule: "Prefer compact correction over expanding the artifact."  no_silent_conflict_resolution:    required: true    rule: "Resolve mechanical issues, but surface priority, deadline, assignment, and blocker uncertainty."
```

# Minimal Validation Record

```
validation_record_template:  status: "operator_review_needed | valid | invalid"  checks:    rating_format_valid: true    priority_range_valid: true    urgency_range_valid: true    date_format_valid: true    every_task_belongs_to_project_or_unassigned: true    unassigned_removed_after_assignment: true    no_workstreams: true    no_detailed_project_state_expansion: true    blockers_only_when_present: true    manual_override_preserved: true    deadline_first_ranking_applied: true  review_flags:    uncertain_ratings: []    invalid_dates: []    assignment_uncertainties: []    duplicate_items: []    ranking_conflicts: []    blocker_uncertainties: []
```

```
---# VALIDATION CHECKLIST- [ ] Exactly one file was produced.- [ ] Ranking rule is deadline-first with manual override.- [ ] Validation checks match the compact model.- [ ] Failure modes prevent over-engineering.
```