Based on thorough examination of the Hermes Agent repository, here is the complete verification report grounded in repo evidence.

---

```
verification_report:      repo_basis:      checked_sources:        - path_or_doc: AGENTS.md (lines 809-882)          relevance: cron invariants, kanban dispatcher, profile isolation        - path_or_doc: website/docs/user-guide/features/cron.md          relevance: cron constraints, context_from, memory skip, recursion guard        - path_or_doc: website/docs/user-guide/features/kanban.md          relevance: workspace rules, dispatcher, idempotency, human gates        - path_or_doc: website/docs/user-guide/profiles.md          relevance: profile isolation model, toolsets, SOUL.md        - path_or_doc: website/docs/user-guide/features/memory.md          relevance: MEMORY.md/USER.md frozen snapshot, cron skip        - path_or_doc: skills/devops/kanban-orchestrator/SKILL.md          relevance: orchestrator pattern, anti-temptation rules, profile discovery        - path_or_doc: skills/devops/kanban-worker/SKILL.md          relevance: worker lifecycle, workspace kinds, review-required convention        - path_or_doc: CONTRIBUTING.md (lines 331-420)          relevance: SKILL.md required frontmatter fields and validator        - path_or_doc: tools/skill_manager_tool.py (_validate_frontmatter)          relevance: enforced SKILL.md constraints (name, description, body)        - path_or_doc: cron/scheduler.py (_resolve_cron_disabled_toolsets, lines 60-80)          relevance: cronjob toolset disabled in cron; messaging/clarify also disabled        - path_or_doc: cron/scheduler.py (lines 1625-1657)          relevance: skip_memory=True hardcoded in cron AIAgent instantiation        - path_or_doc: hermes_cli/kanban_db.py (resolve_workspace, lines 4543-4604)          relevance: absolute path enforcement for dir: and worktree workspaces        - path_or_doc: hermes_cli/kanban_db.py (create_task, lines 1995-2232)          relevance: idempotency_key logic, parent promotion, task schema        - path_or_doc: hermes_cli/config.py (lines 1849-1901)          relevance: kanban config defaults (orchestrator_profile, failure_limit, etc.)        - path_or_doc: hermes_cli/kanban_decompose.py (lines 180-214)          relevance: orchestrator_profile vs default_assignee resolution        - path_or_doc: website/docs/developer-guide/cron-internals.md (lines 199-208)          relevance: recursion guard — cronjob toolset disabled in cron sessions        - path_or_doc: tools/kanban_tools.py (KANBAN_COMPLETE_SCHEMA, lines 960-1051)          relevance: kanban_complete artifacts field, metadata free-form dict        - path_or_doc: website/docs/user-guide/features/kanban-worker-lanes.md          relevance: env vars injected into workers, lifecycle terminator contract      executive_verdict:      compatible_overall: mostly      main_reason: >        The core loop decomposition maps cleanly onto Hermes primitives.        FlowRecapSkill is correctly scoped as a skill. OperatorExecutesPlannedFlow        is correctly excluded from Hermes runtime. The main incompatibilities are        (1) cron's 3-minute hard interrupt makes it unsafe for heavy synthesis        directly inside a cron session, (2) cron sessions skip memory by design        so any cron-triggered process cannot read MEMORY.md/USER.md, and (3)        workspace paths for dir: workspaces must be absolute before runtime        translation — logical location slots are insufficient at dispatch time.      biggest_blocker: >        No profile names are defined yet. The Kanban dispatcher silently fails        to spawn unknown assignee names — tasks sit in ready forever with no        error. Every Kanban task in the design must be assigned to a profile        that actually exists on the machine before runtime translation.      hermes_native_mapping:        PreCapWeek:        recommended_runtime_target: operator_triggered_skill        reason: >          PreCapWeek is a weekly planning synthesis that reads prior context and          writes a weekly_plan_packet. It is operator-initiated (not recurring          enough to justify cron), single-profile, and produces a structured          artifact. A SKILL.md with a procedure section is the correct primitive.          If it needs to survive restarts or involve multiple profiles, escalate          to a Kanban task created manually by the operator.        constraints: >          Skill must be self-contained. The prompt must reference all input          artifact paths explicitly since cron sessions (if ever used) cannot          read MEMORY.md. If run as a cron job, the 3-minute hard interrupt          applies — heavy synthesis must complete within that window or the          session is killed.        PreCapNextDay:        recommended_runtime_target: operator_triggered_skill_or_cron_creates_kanban_task        reason: >          PreCapNextDay reads many inputs (weekly plan, all-project status,          FlowRecaps, calendar, model usage, AI surface inventory) and writes          four flow packets plus prompt packets. This is too heavy for a direct          cron session (3-minute hard interrupt). The most Hermes-native options          are: (A) operator manually invokes a skill that does the synthesis          interactively, or (B) a cron job fires at a fixed time and creates a          Kanban task assigned to a planning profile, which then does the          synthesis with no time limit. Option A is simpler for a personal          system. Option B adds durability and audit trail.        constraints: >          If using cron: the cron session cannot read MEMORY.md (skip_memory=True).          All context must be injected via context_from (reads prior cron output,          max 8K chars truncated) or via workdir (loads AGENTS.md from that dir).          If using Kanban: the planning profile must exist on the machine before          runtime. The workspace must use dir:<absolute_path> to persist the          output artifacts.        OperatorExecutesPlannedFlow:        recommended_runtime_target: human_action_not_Hermes_process        reason: >          Correctly excluded. Hermes has no primitive for "human executes a          planned sequence." The operator reads the flow packet and prompt          packets, runs the AI interactions manually, and produces a raw_flow_dump.          This is a design-layer boundary, not a Hermes runtime gap.        FlowRecapSkill:        recommended_runtime_target: hermes_skill        reason: >          FlowRecapSkill is correctly scoped as a Hermes skill. It is repeatable,          operator-triggered after each flow, single-profile by default, consumes          structured inputs (planned flow packet + raw dump), and outputs a          structured recap packet. The operator-validates-next-step requirement          maps to the kanban_block(reason="review-required: ...") pattern if          running inside a Kanban task, or to a simple interactive confirmation          if running as a skill in a chat session.        escalation_conditions: >          Escalate to a Kanban task when: (1) the recap involves multiple profiles          (e.g., a separate analyst profile validates the next step), (2) the          recap must survive a crash or restart, (3) the audit trail of every          FlowRecap must be queryable later. For a personal single-profile system,          a skill is sufficient.        AllProjectStatusPacketUpdate:        recommended_runtime_target: operator_triggered_skill_or_cron_creates_kanban_task        reason: >          APSU runs once daily or manually, merges FlowRecaps since last update,          and writes the updated all_project_status_packet. For a personal system,          an operator-triggered skill is the simplest safe choice. If scheduling          is desired, a cron job that ONLY creates a Kanban task (not one that          does the synthesis directly) is safer because: (1) the 3-minute cron          limit may be insufficient for merging multiple FlowRecaps, (2) cron          sessions cannot read MEMORY.md, and (3) a Kanban task gives the          operator a human review gate before the packet is finalized.        constraints: >          Do NOT run heavy APSU synthesis directly inside a cron session.          If using cron: use no_agent=True with a script that checks whether          FlowRecaps exist since last update, then emit wakeAgent=true only          when there is work to do — this avoids burning LLM tokens on empty          ticks. The cron job should create a Kanban task rather than synthesize          directly. The Kanban task's workspace must use dir:<absolute_path>          so the output packet persists.        ModelSubscriptionUsageTracking:        recommended_runtime_target: skill_or_file_based_tracking        reason: >          Usage tracking is a lightweight bookkeeping concern. It does not need          Kanban durability or cron scheduling. A skill that reads/writes a          structured YAML or JSON file at a known absolute path is sufficient.          If daily summaries are needed automatically, a no_agent=True cron job          with a script that aggregates the usage file is the right primitive          (zero LLM tokens, just script stdout delivered).        constraints: >          The usage file must be at an absolute path known to all processes that          write to it. Define the path before runtime translation. The          routing_recommendation_packet is a design artifact — it does not map          to a Hermes primitive and must be implemented as a file the operator          reads manually or that a skill generates on demand.      invalid_or_risky_assumptions:        - assumption: Memory (MEMORY.md/USER.md) is available in cron sessions        status: invalid        repo_basis: >          cron/scheduler.py line 1652: skip_memory=True is hardcoded in the          AIAgent instantiation for every cron job. AGENTS.md line 836-837:          "Cron sessions pass skip_memory=True by default; memory providers          intentionally do not run during cron."        correction: >          Any cron-triggered process cannot read MEMORY.md or USER.md. All          context that a cron job needs must be embedded in the prompt itself,          loaded via context_from (prior cron output, max 8K chars), or loaded          via workdir (AGENTS.md from a project directory). SOUL.md IS loaded          (load_soul_identity=True). Design all cron prompts to be self-contained.        - assumption: Cron workers can create additional cron jobs        status: invalid        repo_basis: >          cron/scheduler.py lines 60-80: _resolve_cron_disabled_toolsets()          always includes "cronjob" in the disabled list. cron.md warning:          "Cron-run sessions cannot recursively create more cron jobs. Hermes          disables cron management tools inside cron executions."          cron-internals.md lines 199-204: "Recursion Guard — Cron-run sessions          have the cronjob toolset disabled."        correction: >          A cron job cannot schedule other cron jobs. It CAN create Kanban tasks          (if the kanban toolset is enabled for the cron platform). Design any          "cron creates downstream work" pattern as cron → kanban_create, not          cron → cronjob.        - assumption: Unknown Kanban assignee names produce an error or fallback        status: invalid        repo_basis: >          skills/devops/kanban-orchestrator/SKILL.md line 20: "The dispatcher          silently fails to spawn unknown assignee names — it doesn't autocorrect,          doesn't suggest, doesn't fall back. So a card assigned to 'researcher'          on a setup that only has 'docker-worker' just sits in 'ready' forever."          kanban.md line 69: "After kanban.failure_limit consecutive spawn          failures on the same task (default: 2) the dispatcher auto-blocks it."        correction: >          All profile names used as Kanban assignees must be verified to exist          via 'hermes profile list' before creating tasks. Set kanban.default_assignee          in config.yaml as a safety net. Never invent profile names in design          documents — they must match actual installed profiles.        - assumption: dir: workspace paths can be relative or logical slots        status: invalid        repo_basis: >          hermes_cli/kanban_db.py resolve_workspace lines 4584-4590: raises          ValueError for non-absolute workspace_path with message "use an          absolute path (relative paths are ambiguous against the dispatcher's          CWD)". kanban.md line 67: "Must be an absolute path. Relative paths          like dir:../tenants/foo/ are rejected at dispatch."          tests/hermes_cli/test_kanban_core_functionality.py line 2535-2551          confirms rejection.        correction: >          All dir: and worktree workspace paths must be absolute before runtime          translation. Logical location slots are acceptable at design phase but          must be resolved to absolute paths before any Kanban task is created.          scratch workspaces auto-resolve under ~/.hermes/kanban/workspaces/<id>/          but are deleted on task completion — do not use scratch for persistent          artifacts.        - assumption: kanban.orchestrator_profile and root task assignee are the same concept        status: invalid        repo_basis: >          hermes_cli/config.py lines 1868-1873: "Profile that decomposes tasks          in the Triage column. When unset, falls back to the default profile."          hermes_cli/kanban_decompose.py lines 180-198: _resolve_orchestrator_profile()          reads kanban.orchestrator_profile from config.yaml — this is the profile          that runs the auto-decomposer on triage tasks, not the assignee of any          specific task.        correction: >          kanban.orchestrator_profile is a global config setting that controls          which profile runs the triage decomposer. The root task assignee is          set per-task in kanban_create(assignee=...). They are independent.          A task assigned to "meta_operations" does not require          kanban.orchestrator_profile="meta_operations".        - assumption: Cron can safely do heavy multi-input synthesis within its session        status: risky        repo_basis: >          AGENTS.md line 830-831: "3-minute hard interrupt on cron sessions —          runaway agent loops cannot monopolize the scheduler."          skills/autonomous-ai-agents/hermes-agent/SKILL.md line 652:          "3-minute hard interrupt per run."        correction: >          PreCapNextDay and APSU synthesis (reading multiple FlowRecaps, merging          status packets, generating four flow packets) will likely exceed 3          minutes for non-trivial inputs. Use cron only to CREATE a Kanban task;          let the Kanban worker (no time limit, with heartbeat) do the synthesis.        - assumption: Cron sessions have access to project context files by default        status: risky        repo_basis: >          cron/scheduler.py line 1650: skip_context_files=not bool(_job_workdir).          Without a workdir set on the cron job, AGENTS.md/CLAUDE.md/.cursorrules          are NOT loaded. SOUL.md is always loaded (load_soul_identity=True).        correction: >          Set workdir=<absolute_path> on any cron job that needs project context          files. Note: workdir-pinned jobs run serially (not in the parallel pool)          per cron.md line 125-126.        - assumption: Profiles have a fixed roster of toolsets        status: needs_decision        repo_basis: >          AGENTS.md lines 723-738: toolsets are defined in toolsets.py and          enabled/disabled per platform via config.yaml tools.<platform>.enabled          / tools.<platform>.disabled. There is no fixed roster — each profile          configures its own toolsets.        correction: >          Decide which toolsets each profile needs before runtime. An orchestrator          profile should enable 'kanban' but disable 'terminal', 'file', 'code',          'web' to enforce the "route, don't execute" rule. Worker profiles need          'terminal', 'file', 'skills'. The kanban toolset is auto-enabled for          dispatcher-spawned workers via HERMES_KANBAN_TASK env var.        - assumption: Delegation (delegate_task) is durable and survives restarts        status: invalid        repo_basis: >          AGENTS.md lines 769-771: "Synchronicity rule: delegate_task is NOT          durable. For long-running work that must outlive the current turn,          use cronjob or terminal(background=True, notify_on_complete=True)."        correction: >          Do not use delegate_task for any step in the core loop that must          survive a crash. Use Kanban for durable cross-profile handoffs.      blocking_gaps_before_runtime_translation:        - gap: No profile names defined        why_blocking: >          The Kanban dispatcher silently fails on unknown assignee names. Every          Kanban task in the design (FlowRecapSkill as Kanban task, APSU task,          PreCapNextDay task) must be assigned to a profile that exists on the          machine. Without defined profile names, no Kanban task can be created.        minimal_resolution: >          Define the profile name(s) for this personal system. For a single-operator          system, one profile (e.g., "default" or "alfred") may handle all roles.          Run 'hermes profile list' at runtime setup to confirm names before          creating any Kanban tasks.        - gap: No absolute workspace paths defined for persistent artifacts        why_blocking: >          dir: workspaces require absolute paths at task creation time. The design          uses logical location slots (e.g., "weekly_plan_packet storage") which          cannot be used directly in kanban_create(workspace_path=...).        minimal_resolution: >          Before runtime translation, resolve each logical location slot to an          absolute path (e.g., /home/user/hermes-ops/artifacts/). These paths          must exist on disk. Use a single root directory (e.g., ~/hermes-ops/)          with subdirectories per artifact type.        - gap: No idempotency keys defined for recurring Kanban tasks        why_blocking: >          APSU and PreCapNextDay may be triggered multiple times (cron + manual).          Without idempotency keys, duplicate tasks accumulate in the board.          hermes_cli/kanban_db.py line 2027-2119 shows idempotency_key dedup          logic: first create wins, subsequent calls return existing task id.        minimal_resolution: >          Define idempotency key patterns before runtime. Example:          "apsu-$(date -u +%Y-%m-%d)" for daily APSU tasks,          "precap-$(date -u +%Y-%m-%d)" for daily PreCapNextDay tasks.          Pass these as --idempotency-key on kanban create or in kanban_create().        - gap: FlowRecapSkill operator-validates-next-step has no Hermes primitive        why_blocking: >          The design requires the operator to validate the next step before it          is written to context_for_future_PreCapNextDay. Hermes has no "pause          and wait for operator approval" primitive in a skill session. In a          Kanban task, kanban_block(reason="review-required: ...") is the correct          primitive, but it requires the task to be running inside a Kanban worker.        minimal_resolution: >          For a skill-based FlowRecapSkill: the skill writes a draft next_step_proposal          to a file and asks the operator interactively (in the chat session) to          confirm or edit. The operator's response is then written as          operator_validated_next_step. This works in an interactive CLI session.          For a Kanban-based FlowRecapSkill: use kanban_block(reason="review-required:          validate next step proposal") + kanban_comment with the proposal, then          operator unblocks with their decision as a comment.        - gap: ModelSubscriptionUsageTracking schema has undefined fields        why_blocking: >          The design mentions confidence: medium_high (enum conflict), reserve_for          (ambiguous), source_update_id (undefined), daily_summary_file (unclear          owner), actual_usage_delta (unclear), available_AI_surface_inventory          (undefined), storage root (undefined). These are not Hermes blockers          per se — Hermes has no schema enforcement for free-form YAML files —          but they must be resolved before the skill can write valid artifacts.        minimal_resolution: >          Define the schema as a simple YAML file at a fixed absolute path.          Remove confidence enums (use plain strings). Define actual_usage_delta          as a dict of {model_name: {tokens_in: N, tokens_out: N, cost_usd: N}}.          Define available_AI_surface_inventory as a list of {name, provider,          context_window, cost_per_1k_tokens, status}. The skill owns writing          the daily_summary_file; the path is a config constant.        - gap: Cron 3-minute limit incompatible with heavy synthesis        why_blocking: >          PreCapNextDay and APSU synthesis are described as reading multiple          artifacts and generating complex structured outputs. These will exceed          3 minutes for non-trivial inputs. Running them directly in a cron          session will result in silent truncation/interruption.        minimal_resolution: >          Redesign cron-triggered steps as: cron job fires → creates Kanban task          → Kanban worker (no time limit) does the synthesis. The cron job itself          only needs to run hermes kanban create with the right parameters, which          takes seconds.      artifact_graph_review:      artifacts:          - artifact_id: weekly_plan_packet          producer: PreCapWeek (skill or operator)          consumers: [PreCapNextDay]          embedded_or_separate_recommendation: >            Separate YAML or Markdown file at a fixed absolute path. YAML embedded            in Markdown is acceptable for human readability. Must be at an absolute            path known to PreCapNextDay.          logical_location_slot: <hermes_ops_root>/plans/weekly/YYYY-WNN.md          required_validation: >            Must contain: week_id, date_range, project_priorities (list),            flow_allocation (F1-F4 per day), constraints. No Hermes schema            enforcement — validation is design-layer only.          missing_input_behavior: >            PreCapNextDay should use first-cycle fallback (generate a minimal            weekly plan from scratch) or ask operator. Do not block hard on            first run.          lifecycle_notes: >            One file per week. Overwritten if PreCapWeek is re-run for the same            week. Archive after the week ends.          - artifact_id: next_day_plan          producer: PreCapNextDay          consumers: [OperatorExecutesPlannedFlow, FlowRecapSkill]          embedded_or_separate_recommendation: >            Separate Markdown file. Contains the four flow packets and prompt            packets as sections. Must be at an absolute path.          logical_location_slot: <hermes_ops_root>/plans/daily/YYYY-MM-DD.md          required_validation: >            Must contain: date, F1-F4 flow packets, prompt_packets per flow,            embedded_context_instructions, FlowRecapSkill_instruction_blocks.          missing_input_behavior: >            If weekly_plan_packet is missing: use first-cycle fallback.            If all_project_status_packet is missing: use first-cycle fallback            (assume all projects at baseline).          lifecycle_notes: >            One file per day. Overwritten if PreCapNextDay is re-run for the            same day. Use idempotency key on any Kanban task that creates it.          - artifact_id: flow_packet          producer: PreCapNextDay          consumers: [OperatorExecutesPlannedFlow, FlowRecapSkill]          embedded_or_separate_recommendation: >            Embedded as a section within next_day_plan. Can also be extracted            as a separate file per flow (F1-F4) for operator convenience.          logical_location_slot: section within next_day_plan or <hermes_ops_root>/flows/YYYY-MM-DD-F{N}.md          required_validation: >            Must contain: flow_id (F1-F4), project, purpose, S1/S2/S3 sprint            definitions, expected_outputs, context_instructions.          missing_input_behavior: block — operator must run PreCapNextDay first          lifecycle_notes: ephemeral per day; consumed by FlowRecapSkill after flow          - artifact_id: prompt_packet          producer: PreCapNextDay          consumers: [OperatorExecutesPlannedFlow]          embedded_or_separate_recommendation: >            Embedded within flow_packet or next_day_plan. Separate file only            if operator needs to copy-paste into a different AI surface.          logical_location_slot: section within flow_packet          required_validation: >            Must contain: target_model, target_surface, prompt_text,            expected_output_format, context_to_inject.          missing_input_behavior: block — operator cannot execute flow without prompt          lifecycle_notes: consumed once per flow execution; no persistence needed after          - artifact_id: raw_flow_dump          producer: OperatorExecutesPlannedFlow (human writes this)          consumers: [FlowRecapSkill]          embedded_or_separate_recommendation: >            Separate Markdown file. The operator pastes or writes the raw output            of the flow execution here. Must be at an absolute path that            FlowRecapSkill can read.          logical_location_slot: <hermes_ops_root>/flows/raw/YYYY-MM-DD-F{N}-dump.md          required_validation: >            Minimum: non-empty, references the flow_id. No strict schema —            FlowRecapSkill reads it as unstructured text.          missing_input_behavior: >            FlowRecapSkill should block (kanban_block) or ask operator if            raw_flow_dump is missing. Do not proceed with recap without it.          lifecycle_notes: >            Written by operator after flow execution. Read once by FlowRecapSkill.            Archive after FlowRecapSkill completes.          - artifact_id: flow_recap_packet          producer: FlowRecapSkill          consumers: [AllProjectStatusPacketUpdate, PreCapNextDay (next cycle)]          embedded_or_separate_recommendation: >            Separate YAML-in-Markdown file. Must be at an absolute path.            The YAML frontmatter carries structured fields; the Markdown body            carries the human-readable recap.          logical_location_slot: <hermes_ops_root>/recaps/YYYY-MM-DD-F{N}-recap.md          required_validation: >            Must contain: flow_id, date, project, project_status_delta,            model_usage_delta, artifact_index, prompt_result_summary,            reusable_learning, next_step_proposal, operator_validated_next_step,            context_for_future_PreCapNextDay.          missing_input_behavior: >            APSU should use continue_with_stale_marker if a FlowRecap is missing            for a flow that was supposed to run. Log the gap in skipped_flow_marker.          lifecycle_notes: >            One file per flow per day. Consumed by APSU once. After APSU runs,            mark as consumed in consumed_flow_recap_registry.          - artifact_id: project_status_delta          producer: FlowRecapSkill          consumers: [AllProjectStatusPacketUpdate]          embedded_or_separate_recommendation: >            Embedded as YAML section within flow_recap_packet. No separate file needed.          logical_location_slot: section within flow_recap_packet          required_validation: >            Must contain: project_id, delta_type (progress/blocker/decision/artifact),            delta_description, confidence.          missing_input_behavior: APSU skips delta for that flow; logs gap          lifecycle_notes: consumed by APSU; no independent lifecycle          - artifact_id: model_usage_delta          producer: FlowRecapSkill          consumers: [ModelSubscriptionUsageTracking, AllProjectStatusPacketUpdate]          embedded_or_separate_recommendation: >            Embedded as YAML section within flow_recap_packet. ModelSubscriptionUsageTracking            reads it from there.          logical_location_slot: section within flow_recap_packet          required_validation: >            Must contain: model_name, provider, tokens_in, tokens_out,            cost_usd (estimated), surface_used.          missing_input_behavior: ModelSubscriptionUsageTracking skips; logs gap          lifecycle_notes: consumed by APSU and usage tracker; no independent lifecycle          - artifact_id: all_project_status_packet          producer: AllProjectStatusPacketUpdate          consumers: [PreCapNextDay, PreCapWeek]          embedded_or_separate_recommendation: >            Separate YAML-in-Markdown file at a fixed absolute path (not dated —            always the "current" packet, with version history in git or archived copies).          logical_location_slot: <hermes_ops_root>/status/all_projects_current.md          required_validation: >            Must contain: last_updated_at, projects (list with id, name, status,            last_flow_date, next_step, blockers, artifacts), model_usage_summary,            skipped_flows_since_last_update.          missing_input_behavior: >            PreCapNextDay first-cycle fallback: generate from scratch with            operator-provided project list. Do not block hard on first run.          lifecycle_notes: >            Overwritten on each APSU run. Previous version should be archived            (copy to <hermes_ops_root>/status/archive/YYYY-MM-DD.md) before overwrite.            Use idempotency key on APSU Kanban task to prevent duplicate runs.          - artifact_id: next_PreCapNextDay_input_context          producer: AllProjectStatusPacketUpdate (and FlowRecapSkill)          consumers: [PreCapNextDay]          embedded_or_separate_recommendation: >            Embedded as a section within all_project_status_packet or as a            separate context file. If separate, must be at an absolute path.            Can be embedded YAML in Markdown.          logical_location_slot: section within all_project_status_packet or <hermes_ops_root>/context/next_precap_context.md          required_validation: >            Must contain: key_decisions_since_last_precap, open_blockers,            model_routing_recommendations, priority_shifts.          missing_input_behavior: PreCapNextDay continues without it (uses stale marker)          lifecycle_notes: overwritten each APSU run; consumed by next PreCapNextDay          - artifact_id: skipped_flow_marker          producer: OperatorExecutesPlannedFlow (human marks skip) or APSU          consumers: [AllProjectStatusPacketUpdate, PreCapNextDay]          embedded_or_separate_recommendation: >            Separate YAML file or entries in a registry file. Simple structure.          logical_location_slot: <hermes_ops_root>/markers/skipped_flows.yaml          required_validation: >            Must contain: flow_id, date, project, reason, marked_by, marked_at.          missing_input_behavior: APSU assumes no skips if file missing (first-cycle fallback)          lifecycle_notes: append-only; entries archived after APSU processes them          - artifact_id: consumed_flow_recap_registry          producer: AllProjectStatusPacketUpdate          consumers: [AllProjectStatusPacketUpdate (next run)]          embedded_or_separate_recommendation: >            Separate YAML file listing flow_recap_packet ids/paths that have            been consumed by APSU. Prevents double-counting.          logical_location_slot: <hermes_ops_root>/registry/consumed_recaps.yaml          required_validation: >            Must contain: list of {recap_id, consumed_at, apsu_run_date}.          missing_input_behavior: APSU treats all recaps as unconsumed (first-cycle fallback)          lifecycle_notes: append-only; prune entries older than 30 days          - artifact_id: skipped_flow_marker_registry          producer: AllProjectStatusPacketUpdate          consumers: [AllProjectStatusPacketUpdate (next run), PreCapNextDay]          embedded_or_separate_recommendation: same as skipped_flow_marker — can be merged          logical_location_slot: <hermes_ops_root>/markers/skipped_flows.yaml          required_validation: same as skipped_flow_marker          missing_input_behavior: first-cycle fallback (assume no skips)          lifecycle_notes: same as skipped_flow_marker          - artifact_id: usage_summary          producer: ModelSubscriptionUsageTracking          consumers: [PreCapNextDay, AllProjectStatusPacketUpdate]          embedded_or_separate_recommendation: >            Separate YAML file at a fixed absolute path. Updated after each            FlowRecapSkill run. Daily summary appended to a log file.          logical_location_slot: <hermes_ops_root>/usage/current_summary.yaml          required_validation: >            Must contain: period_start, period_end, by_model (list of            {model_name, provider, total_tokens_in, total_tokens_out,            total_cost_usd, session_count}), routing_recommendations.            Remove confidence enum — use plain string (high/medium/low).          missing_input_behavior: PreCapNextDay continues without routing recommendations          lifecycle_notes: overwritten daily; daily snapshots archived to usage/archive/          - artifact_id: AI_surface_inventory          producer: operator (manually maintained)          consumers: [PreCapNextDay, ModelSubscriptionUsageTracking]          embedded_or_separate_recommendation: >            Separate YAML file at a fixed absolute path. Manually maintained            by operator. Not generated by any Hermes process.          logical_location_slot: <hermes_ops_root>/config/ai_surface_inventory.yaml          required_validation: >            Must contain: list of {name, provider, model_id, context_window_tokens,            cost_per_1k_input_tokens, cost_per_1k_output_tokens, status (active/paused/quota_exceeded),            subscription_tier, monthly_budget_usd, notes}.            Remove reserve_for ambiguity — use explicit fields.          missing_input_behavior: PreCapNextDay uses default routing (no optimization)          lifecycle_notes: manually updated; no automated writes      trigger_and_cadence_review:        PreCapNextDay_trigger:        recommended: >          Option A (simpler): Operator manually invokes a skill in an interactive          session each morning. The skill reads all inputs from known absolute paths          and writes the next_day_plan.          Option B (automated): A cron job fires at a fixed time (e.g., "0 7 * * *")          and creates a Kanban task assigned to the planning profile. The Kanban          worker does the synthesis with no time limit.        avoid: >          Do NOT run PreCapNextDay synthesis directly inside a cron session.          The 3-minute hard interrupt will kill the session before synthesis completes          for non-trivial inputs. Do NOT use delegate_task — it is not durable.        reason: >          cron/scheduler.py line 1652 confirms skip_memory=True. AGENTS.md line 830          confirms 3-minute hard interrupt. For a personal system, operator-triggered          skill is the lowest-friction option.        APSU_trigger:        recommended: >          Option A (simplest): Operator manually invokes a skill once daily after          all flows are complete. The skill reads all FlowRecaps since last APSU          run and writes the updated all_project_status_packet.          Option B (automated): A cron job fires once daily (e.g., "0 22 * * *")          and creates a Kanban task with idempotency key "apsu-$(date -u +%Y-%m-%d)".          The Kanban worker does the merge.        avoid: >          Do NOT run APSU synthesis directly inside a cron session (3-minute limit).          Do NOT run APSU after every FlowRecap (design correctly excludes this).          Do NOT create duplicate APSU tasks — use idempotency keys.        reason: >          The design correctly specifies "explicitly_not_after_every_flow: true".          The cron 3-minute limit is the key constraint. Idempotency keys prevent          duplicate runs when both cron and manual triggers fire on the same day.      profile_and_toolset_review:      profiles:          - profile: Alfred (or default)          should_exist: yes          role: >            Primary operator-facing profile. Handles interactive skill invocations            (FlowRecapSkill, PreCapNextDay as skill, APSU as skill). Has memory            enabled. Runs the gateway for messaging delivery.          required_toolsets_later: >            terminal, file, skills, memory, session_search, kanban (if orchestrating),            cronjob (for scheduling), web (optional for research flows)          risk: >            Low. This is the standard single-profile setup. No over-proliferation risk.          - profile: meta_operations          should_exist: maybe          role: >            If APSU and PreCapNextDay are run as Kanban workers, this profile            handles operational synthesis tasks. If the system stays single-profile,            this is unnecessary — Alfred handles everything.          required_toolsets_later: >            file, skills, memory, kanban (worker toolset auto-enabled by dispatcher)          risk: >            Over-proliferation risk for a personal system. A second profile adds            complexity (separate config, API keys, gateway). Only create if there            is a genuine reason to isolate this profile's model/skills from Alfred.          - profile: meta_strategist          should_exist: maybe          role: >            If PreCapWeek and strategic validation are separated from operational            synthesis. For a personal system, this is almost certainly unnecessary.          required_toolsets_later: file, skills, memory          risk: >            High over-proliferation risk. For a personal single-operator system,            one profile handles all roles. Multiple profiles are justified when            different models, different API keys, or different skill sets are needed            per role. Do not create profiles for conceptual separation alone.          - profile: meta_detective_controller          should_exist: maybe          role: >            If FlowRecapSkill needs a separate profile for analysis/validation.            For a personal system, unnecessary.          required_toolsets_later: file, skills, memory          risk: >            High over-proliferation risk. Same reasoning as meta_strategist.            Consolidate into Alfred or meta_operations unless there is a concrete            model/skill isolation requirement.      minimal_patch_plan:        - patch: Remove confidence enum from ModelSubscriptionUsageTracking schema        target_spec: ModelSubscriptionUsageTracking artifact schema        reason: >          "confidence: medium_high" is not a valid YAML enum value in any Hermes          schema. Hermes has no schema enforcement for free-form YAML files, but          the enum conflict will cause confusion when the skill tries to write it.        exact_change: >          Replace confidence: medium_high with confidence: "medium_high" (plain string)          or define confidence as one of: high | medium | low. Remove the enum          constraint from the design spec.        - patch: Define storage root for all artifacts as a single absolute path constant        target_spec: All artifact logical_location_slots        reason: >          dir: workspaces require absolute paths. All artifact paths must be          resolvable before runtime. A single root (e.g., ~/hermes-ops/) with          subdirectories eliminates ambiguity.        exact_change: >          Add to design: HERMES_OPS_ROOT=/home/<user>/hermes-ops/ (or equivalent).          All logical_location_slots resolve relative to this root. This path          must exist on disk before any Kanban task is created.        - patch: Add idempotency_key to APSU and PreCapNextDay Kanban task specs        target_spec: AllProjectStatusPacketUpdate and PreCapNextDay runtime specs        reason: >          Without idempotency keys, cron + manual triggers on the same day create          duplicate tasks. hermes_cli/kanban_db.py line 2027-2119 shows the          idempotency mechanism is available and ready to use.        exact_change: >          APSU idempotency_key: "apsu-{YYYY-MM-DD}"          PreCapNextDay idempotency_key: "precap-{YYYY-MM-DD}"          These are passed as --idempotency-key on kanban create or in kanban_create().        - patch: Clarify FlowRecapSkill operator-validation gate as kanban_block pattern        target_spec: FlowRecapSkill artifact flow (operator_validated_next_step)        reason: >          The design says "asks operator to validate next step" but does not specify          the Hermes mechanism. In a Kanban context, this is kanban_block(reason=          "review-required: validate next step proposal") + kanban_comment with          the proposal. In a skill context, this is an interactive chat confirmation.        exact_change: >          Add to FlowRecapSkill spec: "If running as Kanban worker: write next_step_proposal          to kanban_comment, then call kanban_block(reason='review-required: validate          next step'). Operator reads comment, adds decision as comment, runs          hermes kanban unblock <id>. Worker respawns and reads operator decision          from comment thread via kanban_show()."        - patch: Define source_update_id in ModelSubscriptionUsageTracking        target_spec: ModelSubscriptionUsageTracking schema        reason: >          source_update_id is referenced but undefined. It is not a Hermes primitive.          It must be defined as a design-layer field.        exact_change: >          Define source_update_id as: "{flow_id}-{YYYY-MM-DD}" — the identifier          of the FlowRecap that produced this usage delta. This allows deduplication          in the usage tracker.        - patch: Add wakeAgent gate to APSU cron job (if cron is used)        target_spec: AllProjectStatusPacketUpdate cron job spec        reason: >          APSU should only run when there are unconsumed FlowRecaps. A pre-run          script that checks for new recaps and emits wakeAgent=false when none          exist prevents burning LLM tokens on empty ticks.          cron.md lines 553-575 documents the wakeAgent gate pattern.        exact_change: >          Add script to APSU cron job: checks count of files in          <hermes_ops_root>/recaps/ not in consumed_flow_recap_registry.          If count == 0: emit {"wakeAgent": false}. Else: emit {"wakeAgent": true}.      open_questions_for_user:        - question: Single profile or multi-profile?        options:          - single_profile: Alfred handles all roles (skill invocations, Kanban tasks)          - two_profiles: Alfred (interactive) + ops-worker (Kanban synthesis tasks)          - four_profiles: Alfred + meta_operations + meta_strategist + meta_detective_controller        recommendation: >          Start with single profile. Add a second profile only if you need a          different model (e.g., a cheaper model for APSU synthesis vs. a smarter          model for PreCapNextDay). The dispatcher silently fails on unknown profile          names, so every profile you name in a Kanban task must exist.        why_it_matters: >          Profile names must be defined before any Kanban task is created.          Over-proliferating profiles adds config complexity with no benefit          for a personal single-operator system.        - question: Should FlowRecapSkill run as a skill or a Kanban task?        options:          - skill: operator invokes interactively; operator-validation is a chat confirmation          - kanban_task: operator creates task; validation is kanban_block/unblock cycle        recommendation: >          Skill for now. The Kanban path adds durability and audit trail but also          adds dispatcher dependency (gateway must be running). For a personal          system where the operator is always present, a skill is simpler and          faster. Migrate to Kanban if you need the audit trail or if FlowRecapSkill          starts taking longer than a single interactive session.        why_it_matters: >          The operator-validates-next-step requirement has different implementations          in each path. Deciding now prevents ambiguity in the SKILL.md authoring.        - question: What is the absolute path for the hermes-ops artifact root?        options:          - ~/hermes-ops/          - ~/Documents/hermes-ops/          - a path inside an Obsidian vault          - a path inside a git repository        recommendation: >          Choose a path inside a git repository so artifact history is tracked.          Example: ~/projects/hermes-ops/ with git init. This also enables          worktree workspaces for Kanban tasks that need git history.        why_it_matters: >          All dir: workspace paths must be absolute. This path must be defined          before any Kanban task is created. It cannot be changed later without          updating all task workspace_path values.        - question: Should PreCapNextDay be cron-triggered or operator-triggered?        options:          - operator_triggered_skill: simplest; operator runs it each morning          - cron_creates_kanban_task: automated; cron fires at 7am, creates task          - cron_direct_synthesis: NOT RECOMMENDED (3-minute limit)        recommendation: >          Operator-triggered skill for the design phase. Add cron automation          later once the skill is stable and the artifact paths are confirmed.          The cron → Kanban task path requires the gateway to be running and          a profile to be defined.        why_it_matters: >          The 3-minute cron limit is a hard constraint. Getting this wrong means          PreCapNextDay silently fails mid-synthesis with no error message to the          operator.        - question: Is RecapDay needed as an optional Alfred layer?        options:          - yes_as_skill: Alfred runs RecapDay as an optional end-of-day skill          - no_not_needed: FlowRecapSkill + APSU cover the recap function          - deferred: keep on_hold as designed        recommendation: >          Keep on_hold as designed. FlowRecapSkill already produces per-flow recaps.          APSU already produces the daily status merge. RecapDay would be redundant          unless it serves a distinct personal review function (e.g., journaling,          mood tracking) that the other skills do not cover.        why_it_matters: >          Adding RecapDay as a required core layer would contradict the design          decision to remove it. Only add it if there is a concrete use case          that FlowRecapSkill + APSU do not cover.      final_recommendation:      next_best_step: >        1. Define the single absolute path for the hermes-ops artifact root           (e.g., ~/projects/hermes-ops/). Create the directory structure.        2. Decide on single-profile vs. two-profile setup. Run 'hermes profile list'           to see what exists. If single-profile, confirm the profile name.        3. Author FlowRecapSkill as a SKILL.md with the required frontmatter           (name, description, body). Test it interactively in a chat session           with a sample raw_flow_dump before adding Kanban complexity.        4. Define idempotency key patterns for APSU and PreCapNextDay.        5. Resolve the ModelSubscriptionUsageTracking schema gaps (remove enum,           define source_update_id, define storage path).        6. Only after steps 1-5 are stable: add cron scheduling and Kanban           task creation for APSU and PreCapNextDay.      do_not_do_yet: >        - Do not create final SKILL.md files until artifact paths are defined.        - Do not create Kanban tasks until profile names are confirmed.        - Do not add cron scheduling until the skill versions are stable.        - Do not create four profiles — start with one or two.        - Do not run PreCapNextDay or APSU synthesis directly inside a cron session.        - Do not use delegate_task for any step that must survive a restart.        - Do not use relative paths for any dir: workspace.
```

---

**Key citations from the repo:**

Cron 3-minute hard interrupt and `skip_memory=True`: AGENTS.md:829-837

Cron `cronjob` toolset disabled (recursion guard): scheduler.py:60-80

Cron `skip_memory=True` hardcoded in AIAgent instantiation: scheduler.py:1650-1656

`dir:` workspace absolute path enforcement: kanban_db.py:4579-4592

Unknown assignee silent failure: SKILL.md:18-21

`kanban.orchestrator_profile` is separate from task assignee: config.py:1868-1877

Idempotency key dedup logic: kanban_db.py:2106-2119

SKILL.md required frontmatter fields (`name`, `description`, non-empty body): skill_manager_tool.py:217-253

Human review gate via `kanban_block` + `review-required:` convention: SKILL.md:50-69

`context_from` chain (reads most recent cron output, max 8K chars): scheduler.py:1045-1090

`scratch` workspace deleted on task completion: kanban.md:66-67

`kanban_complete` artifacts field (absolute paths, gateway uploads): kanban_tools.py:1028-1046

Profile isolation model: AGENTS.md:916-923