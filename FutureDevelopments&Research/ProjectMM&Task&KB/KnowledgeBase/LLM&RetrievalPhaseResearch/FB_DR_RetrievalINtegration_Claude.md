# Phase 1 — Read Ledger

**3-call budget consumed. Files read via MCP:** directory listing + SKILL.md (live). Patch pack read in full from space files. Remaining files (references/*, package-manifest.md, apex_kb.py, templates/, orchestration skills) were **not individually fetched** — marked per status below. The directory listing confirms the repo IS accessible at commit `3f4715807762260aee541e4f92952c5b909754933f4715807762260aee541e4f92952c5b90975493`.

|path|url|status|key_facts|patch_collision_risk|reason|
|---|---|---|---|---|---|
|`.claude/skills/apex-kb/SKILL.md`|[GitHub](https://github.com/leela-spec/apexai-os-meta/blob/3f4715807762260aee541e4f92952c5b90975493/.claude/skills/apex-kb/SKILL.md)|**read**|- Description starts "Use this skill when" ✓; - Has Skill Contract, Supporting Files, Procedure, Failure Modes, Completion Gate ✓; - Procedure has 8 steps ✓; - `query` mode listed in skill_contract.modes; - No retrieval/FTS5 section exists yet; - `output_role: durable_knowledge_base_compiler`|**medium**|Patch proposes appending "## Querying and indexing" section — no collision with existing headings confirmed, but exact tail line must be verified|
|`.claude/skills/apex-kb/package-manifest.md`|[GitHub](https://github.com/leela-spec/apexai-os-meta/blob/3f4715807762260aee541e4f92952c5b90975493/.claude/skills/apex-kb/package-manifest.md)|**not fetched** (budget)|- Size: 13,815 bytes; - Confirmed exists|**medium**|Append collision risk: unknown existing tail structure|
|`.claude/skills/apex-kb/references/kb-contract.md`|[GitHub](https://github.com/leela-spec/apexai-os-meta/tree/3f4715807762260aee541e4f92952c5b90975493/.claude/skills/apex-kb/references)|**not fetched** (budget)|- Directory confirmed exists|**medium**|Section name collision unknown|
|`.claude/skills/apex-kb/references/ingest-query-lint-audit-rules.md`|same dir|**not fetched** (budget)|- Confirmed directory exists|**medium**|Same|
|`.claude/skills/apex-kb/references/script-command-contract.md`|same dir|**not fetched** (budget)|- Confirmed directory exists|**medium**|Proposed append of `## Retrieval command contract` — unknown if heading already present|
|`.claude/skills/apex-kb/templates/ingest-analysis-template.md`|[GitHub](https://github.com/leela-spec/apexai-os-meta/tree/3f4715807762260aee541e4f92952c5b90975493/.claude/skills/apex-kb/templates)|**not fetched** (budget)|- Templates dir confirmed exists|**none**|Template not being patched|
|`apex-meta/scripts/apex_kb.py`|unavailable path (not confirmed)|**not fetched** (budget)|- Not confirmed to exist — no listing of apex-meta/scripts/ done|**high**|Command-parser structure fully unknown; delegation hook cannot be verified|
|`.claude/skills/apex-plan/SKILL.md`|not fetched|**not fetched**|—|**none**|Read-only audit; no patch proposed|
|`.claude/skills/apex-session/SKILL.md`|not fetched|**not fetched**|—|**none**|Same|
|`.claude/skills/PrecapNextDay/SKILL.md`|not fetched|**not fetched**|—|**none**|Same|

> **Budget note:** The 3 MCP call limit was reached after directory listing + SKILL.md fetch. The 8 remaining required files could not be individually verified. All patch content below for unread files is marked `ASSUMPTION_BASED` with explicit risk flags. Only SKILL.md patches are `REPO_VERIFIED`.

---

# Phase 2 — Issue Resolutions

## I-01 / I-02: Patch Anchor Verification

## FILE: `.claude/skills/apex-kb/SKILL.md`

text

``STATUS: safe_to_append INSERTION_POINT: After the final closing YAML block of "Completion Gate"   — last content line is: `    audit:` block ending before EOF COLLISION_DETAIL: none — no "## Querying and indexing" heading exists in live file REPO_VERIFIED: YES (file fully read)``

**VERIFIED_PATCH** — append after final `---` or EOF:

text

``## Retrieval and Index Commands Use this skill's retrieval surface when Claude needs ranked evidence from KB pages, bounded context packets, personal orchestration recall, or cross-project lookup. ### Commands ```yaml retrieval_commands:   build_index:    per_kb: "python apex-meta/scripts/apex_kb_retrieval.py build-index --kb <kb_id>"    global:  "python apex-meta/scripts/apex_kb_retrieval.py build-index --all-kbs"  query:    per_kb: "python apex-meta/scripts/apex_kb_retrieval.py query --kb <kb_id> --query \"<terms>\""    global:  "python apex-meta/scripts/apex_kb_retrieval.py query --all-kbs --query \"<terms>\""    flags:      - "--limit <n>   (default 8)"      - "--write-markdown   (saves artifact to outputs/queries/)"  health:    per_kb: "python apex-meta/scripts/apex_kb_retrieval.py health --kb <kb_id>"    global:  "python apex-meta/scripts/apex_kb_retrieval.py health --all-kbs"  stale:    per_kb: "python apex-meta/scripts/apex_kb_retrieval.py stale --kb <kb_id>"    global:  "python apex-meta/scripts/apex_kb_retrieval.py stale --all-kbs" ``` ### Retrieval rules (binding) - Retrieval is **read-only** — never mutates plans, tasks, or control-state artifacts. - A stale-index check must precede any query relied upon for planning decisions. - Personal orchestration memory uses domain `personal_orchestration` — must not be   collapsed into project KB indexes. - SQLite derived indexes live under `apex-meta/kb/<kb>/derived/search/` — never canonical.``

text

`` --- ### FILE: `.claude/skills/apex-kb/package-manifest.md` ``

STATUS: safe_to_append (ASSUMPTION_BASED — file not read)  
INSERTION_POINT: EOF  
COLLISION_DETAIL: Heading "## Retrieval assets" not expected to exist — not referenced  
anywhere in live SKILL.md. LOW collision probability.  
RISK: medium — if manifest already lists apex_kb_retrieval.py under any heading,  
duplicate entry would result. Operator must grep for "retrieval" before applying.

text

`` **VERIFIED_PATCH:** ```markdown ## Retrieval assets (v1-retrieval) | file | type | role | |---|---|---| | `references/retrieval-contract.md` | reference | Canonical-vs-derived rules, query output contract, domain policy | | `templates/query-output-template.md` | template | Standard markdown artifact for query outputs | | `../../../apex-meta/scripts/apex_kb_retrieval.py` | script | SQLite FTS5 retrieval/index engine — new file, not a replacement | ``` --- ### FILE: `.claude/skills/apex-kb/references/kb-contract.md` ``

STATUS: safe_to_append (ASSUMPTION_BASED)  
INSERTION_POINT: EOF  
COLLISION_DETAIL: Section "## Canonical and derived retrieval artifacts" is new terminology —  
unlikely to collide with ingest-focused contract. If contract already has a "## Derived" or  
"## Index" section, merge rather than append.  
RISK: medium

text

`` **VERIFIED_PATCH:** ```markdown ## Canonical and derived retrieval artifacts **Canonical** (source of truth — never delete or overwrite with derived content): - Markdown wiki pages - Source manifests (`manifests/source-manifest.json`) - Raw source snapshots (`raw/`) - Authored summary pages **Derived** (rebuildable, non-authoritative): - SQLite search indexes (`derived/search/index.sqlite`) - Query result markdown outputs (`outputs/queries/`) Rule: derived artifacts must always be fully rebuildable from canonical artifacts alone. ## Required retrieval metadata fields Minimum required in `documents` index record: | field | type | required | |---|---|---| | `kb_id` | TEXT | yes | | `domain` | TEXT | yes — one of: `project`, `personal_orchestration`, `cross_project` | | `rel_path` | TEXT | yes | | `title` | TEXT | yes | | `page_hash` | TEXT | yes — SHA-256 of raw file content | | `source_mtime` | REAL | yes | Recommended frontmatter keys for pages: ```yaml title: "" domain: ""          # project | personal_orchestration | cross_project tags: [] updated_at: "" status: ""          # draft | active | archived ``` ```` --- ### FILE: `.claude/skills/apex-kb/references/ingest-query-lint-audit-rules.md` ``

STATUS: safe_to_append (ASSUMPTION_BASED)  
INSERTION_POINT: EOF — after existing ingest/lint/audit rule sections  
COLLISION_DETAIL: "query" mode is referenced in SKILL.md but retrieval-specific  
rules are absent (confirmed by SKILL.md read — query procedure step 7 describes  
index-first reading without FTS5 machinery). No FTS5/stale rules exist yet.  
RISK: low-medium

text

`` **VERIFIED_PATCH:** ```markdown ## Retrieval and index rules ### Query mode rules - Query is strictly **read-only**. - Query outputs must cite canonical `rel_path` values — no invented source paths. - Query outputs must prefer bounded `snippet()` excerpts over full page dumps. - Stale-index status must be checked before relying on results for planning decisions. - If stale, the tool must report `"status": "stale"` and prompt operator to rebuild. ### Index integrity rules - FTS5 support must be probed at runtime — never assumed. - Stale detection must cover: added files, deleted files, and modified files (not mtime-max alone). - Index rebuild must be idempotent — repeated builds must produce equivalent state. ### Domain isolation rules - `personal_orchestration` domain queries must not bleed results into project KB scopes. - Global cross-KB queries must attach `kb_id` to every result row. ### Audit rules for retrieval - If index and canonical file-set disagree (added/deleted/modified), report all three delta sets. - A health check with `PRAGMA compile_options` output must be available on demand. ``` --- ### FILE: `.claude/skills/apex-kb/references/script-command-contract.md` ``

STATUS: safe_to_append (ASSUMPTION_BASED)  
INSERTION_POINT: EOF  
COLLISION_DETAIL: Contract documents apex_kb.py commands. New section is for  
apex_kb_retrieval.py — a new file. No overlap expected.  
RISK: low — only risk is if "build-index" already appears as a subcommand in apex_kb.py section.  
Operator must verify apex_kb.py does NOT already have build-index/query/health/stale.

text

``**VERIFIED_PATCH:** ```markdown ## apex_kb_retrieval.py — Retrieval command contract Script path: `apex-meta/scripts/apex_kb_retrieval.py` Runtime: Python 3.9+ stdlib only (optional: `python-frontmatter` for robust YAML parsing) ### Subcommands | subcommand | required args | optional args | stdout | exit codes | |---|---|---|---|---| | `build-index` | `--kb <id>` OR `--all-kbs` | — | JSON: `{status, db_path, scope, results[]}` | 0=ok, 1=error | | `query` | `--kb <id>` OR `--all-kbs`, `--query "<terms>"` | `--limit <n>`, `--write-markdown` | JSON: `{status, query, scope, result_count, results[]}` | 0=ok, 1=error | | `health` | `--kb <id>` OR `--all-kbs` | — | JSON: `{status, fts5_available, compile_options, document_count, chunk_count}` | 0=ok | | `stale` | `--kb <id>` OR `--all-kbs` | — | JSON: `{status, kb_status[{kb_id, added[], deleted[], modified[], stale}]}` | 0=ok, 2=stale, 1=missing | ### Input contract - `--kb <id>`: KB slug matching a directory under `apex-meta/kb/<id>/` - `--all-kbs`: builds/queries the global index at `apex-meta/kb/.global/derived/search/index.sqlite` - `--query`: FTS5 query string — supports `AND`, `OR`, `NOT`, `NEAR`, prefix `*` ### Output contract Every `query` result item must contain: ```json {   "kb_id": "string",  "domain": "string",  "rel_path": "string",  "title": "string",  "section_title": "string",  "heading_path": "string",  "anchor": "string",  "snippet": "string",  "score": "float (lower = more relevant, BM25 ascending)" } ``` ### Index path convention ``` per-KB:  apex-meta/kb/<kb_id>/derived/search/index.sqlite global:  apex-meta/kb/.global/derived/search/index.sqlite``

text

`` --- ### FILE: `apex-meta/scripts/apex_kb.py` ``

STATUS: restructure_required (HIGH RISK — file NOT read, path not confirmed to exist)  
INSERTION_POINT: UNKNOWN — command-parser structure unverified  
COLLISION_DETAIL: Cannot determine if subcommands build-index/query/health/stale  
already exist. Cannot safely patch argparse routing.  
RECOMMENDATION: DO NOT patch apex_kb.py until file is read. Use apex_kb_retrieval.py  
as standalone entrypoint. If delegation is needed after reading the live file, use  
the pattern below ONLY if confirmed safe.

text

``**SAFE DELEGATION PATTERN** (apply only after reading live `apex_kb.py` and confirming none of `build-index`, `query`, `health`, `stale` exist as subcommands): ```python # ── retrieval delegation (safe addition — no existing subcommand conflict) ── import importlib.util as _ilu, pathlib as _pl def _load_retrieval():     _p = _pl.Path(__file__).parent / "apex_kb_retrieval.py"    if not _p.exists():        raise SystemExit("apex_kb_retrieval.py not found; cannot delegate retrieval commands.")    spec = _ilu.spec_from_file_location("apex_kb_retrieval", _p)    mod = _ilu.module_from_spec(spec)    spec.loader.exec_module(mod)    return mod _RETRIEVAL_SUBCOMMANDS = {"build-index", "query", "health", "stale"} # Insert BEFORE existing subparser dispatch in main(): # if len(sys.argv) > 1 and sys.argv in _RETRIEVAL_SUBCOMMANDS:[2] #     sys.exit(_load_retrieval().main(sys.argv[1:])) ``` --- ## O-01: Incremental Index Update — `build_index()` + `build_parser()` replacement Complete replacement for `build_index()` and the `build-index` subparser section. Depends on the schema already having `page_hash` and `source_mtime` columns in `documents` (confirmed in patch pack schema). ```python def build_index(args: argparse.Namespace) -> int:     if args.all_kbs:        kb_ids = list_kb_ids()        db_path = global_db_path()    else:        if not args.kb:            raise SystemExit("Either --kb <id> or --all-kbs must be provided.")        kb_ids = [args.kb]        db_path = per_kb_db_path(args.kb)     con = connect(db_path)    probe_fts5(con)     incremental = getattr(args, "incremental", False)    summary = []     with con:        for kb_id in kb_ids:            if incremental:                result = _incremental_index_kb(con, kb_id)            else:                # Full destructive rebuild for this KB scope only                _clear_scope_batch(con, [kb_id])                result = index_kb(con, kb_id)            summary.append(result)         try:            con.execute("INSERT INTO chunk_fts(chunk_fts) VALUES('optimize')")        except sqlite3.DatabaseError:            pass     payload = {        "status": "ok",        "db_path": str(db_path.relative_to(REPO_ROOT)),        "scope": "global" if args.all_kbs else "kb",        "mode": "incremental" if incremental else "full",        "results": summary,    }    print(json.dumps(payload, indent=2))    return 0 def _incremental_index_kb(con: sqlite3.Connection, kb_id: str) -> dict:     """    Compares on-disk file set against indexed records.    - Skips files whose page_hash matches the indexed record.    - Re-indexes changed files (delete chunks + reindex).    - Adds new files.    - Removes stale records for deleted files.    Returns a result dict identical in shape to index_kb().    """    root = kb_dir_for(kb_id)    if not root.exists():        return {            "kb_id": kb_id,            "indexed_documents": 0,            "indexed_chunks": 0,            "missing": True,            "incremental": True,            "added": 0,            "updated": 0,            "deleted": 0,            "unchanged": 0,        }     # Build disk file set: rel_path -> (abs_path, mtime, hash)    disk_files: dict[str, tuple[Path, float, str]] = {}    for abs_path in iter_markdown_files(kb_id):        rel = str(abs_path.relative_to(root))        raw = load_text(abs_path)        disk_files[rel] = (abs_path, abs_path.stat().st_mtime, page_hash(raw))     # Build indexed set: rel_path -> (doc_id, page_hash)    indexed: dict[str, tuple[int, str]] = {        row["rel_path"]: (row["id"], row["page_hash"])        for row in con.execute(            "SELECT id, rel_path, page_hash FROM documents WHERE kb_id = ?", (kb_id,)        )    }     disk_set = set(disk_files.keys())    indexed_set = set(indexed.keys())     to_add = disk_set - indexed_set    to_delete = indexed_set - disk_set    to_check = disk_set & indexed_set     to_update = {        rel for rel in to_check        if disk_files[rel] != indexed[rel]  # hash mismatch[2][1]    }    unchanged = to_check - to_update     # Delete removed files    for rel in to_delete:        doc_id = indexed[rel]        chunk_ids = [            r["id"] for r in con.execute(                "SELECT id FROM chunks WHERE doc_id = ?", (doc_id,)            )        ]        _delete_fts_rows_batched(con, chunk_ids)        con.execute("DELETE FROM chunks WHERE doc_id = ?", (doc_id,))        con.execute("DELETE FROM documents WHERE id = ?", (doc_id,))     # Delete changed files (will be re-indexed below)    for rel in to_update:        doc_id = indexed[rel]        chunk_ids = [            r["id"] for r in con.execute(                "SELECT id FROM chunks WHERE doc_id = ?", (doc_id,)            )        ]        _delete_fts_rows_batched(con, chunk_ids)        con.execute("DELETE FROM chunks WHERE doc_id = ?", (doc_id,))        con.execute("DELETE FROM documents WHERE id = ?", (doc_id,))     # Index new and changed files    indexed_documents = 0    indexed_chunks = 0    for rel in sorted(to_add | to_update):        abs_path, mtime, ph = disk_files[rel]        rel_path = abs_path.relative_to(root)        raw = load_text(abs_path)        meta, body = parse_frontmatter(raw)        title = extract_title(rel_path, body, meta)        domain = infer_domain(kb_id, meta)         cur = con.execute(            """            INSERT INTO documents (kb_id, domain, rel_path, title, source_mtime, page_hash, indexed_at)            VALUES (?, ?, ?, ?, ?, ?, ?)            """,            (kb_id, domain, str(rel_path), title, mtime, ph, utc_now()),        )        doc_id = cur.lastrowid        indexed_documents += 1         for section in split_sections(body, title):            cur = con.execute(                """                INSERT INTO chunks (doc_id, ordinal, section_title, heading_path, anchor, body, token_count)                VALUES (?, ?, ?, ?, ?, ?, ?)                """,                (                    doc_id,                    section.ordinal,                    section.section_title,                    section.heading_path,                    section.anchor,                    section.body,                    token_count(section.body),                ),            )            chunk_id = cur.lastrowid            con.execute(                "INSERT INTO chunk_fts(rowid, title, heading_path, body) VALUES (?, ?, ?, ?)",                (chunk_id, title, section.heading_path, section.body),            )            indexed_chunks += 1     con.execute(        "INSERT INTO index_meta(key, value) VALUES('last_build_utc', ?) "        "ON CONFLICT(key) DO UPDATE SET value = excluded.value",        (utc_now(),),    )     return {        "kb_id": kb_id,        "indexed_documents": indexed_documents,        "indexed_chunks": indexed_chunks,        "missing": False,        "incremental": True,        "added": len(to_add),        "updated": len(to_update),        "deleted": len(to_delete),        "unchanged": len(unchanged),    } def _delete_fts_rows_batched(con: sqlite3.Connection, chunk_ids: list[int], batch_size: int = 900) -> None:     """Delete FTS rows in batches to stay under SQLite's variable limit (999)."""    for i in range(0, len(chunk_ids), batch_size):        batch = chunk_ids[i : i + batch_size]        placeholders = ",".join("?" * len(batch))        con.execute(f"DELETE FROM chunk_fts WHERE rowid IN ({placeholders})", batch) ``` **Updated `build_parser()` section** — replace only the `p_build` subparser block: ```python     p_build = sub.add_parser("build-index", help="Build or incrementally update a KB retrieval index")    p_build.add_argument("--kb", help="KB id (slug under apex-meta/kb/)")    p_build.add_argument("--all-kbs", action="store_true", help="Build global multi-KB index")    p_build.add_argument(        "--incremental",        action="store_true",        help=(            "Skip unchanged files (page_hash match). Re-index changed files. "            "Remove deleted files. Faster than full rebuild for large KBs."        ),    )    p_build.set_defaults(func=build_index) ``` --- ## O-02: `clear_scope()` Replacement Replaces the O(n) Python-loop implementation. Uses batched SQL DELETE with subquery. FK cascade handles chunk and FTS cleanup when `ON DELETE CASCADE` is enforced. ```python _BATCH_SIZE = 900  # stay under SQLite default max variable count (999) def _clear_scope_batch(con: sqlite3.Connection, kb_ids: list[str]) -> None:     """    Batched O(1-per-KB) clear using subquery DELETE.         Schema assumption: chunks.doc_id FK references documents.id ON DELETE CASCADE,    and chunk_fts rows use chunk.id as rowid with manual FTS sync (no content= table).         Because FTS5 virtual tables do NOT respect FK CASCADE automatically, FTS rows    must be deleted explicitly before document rows are removed.    Steps per KB batch:      1. Collect doc_ids for the KB batch.      2. Delete FTS rows for all chunk rowids belonging to those docs (batched).      3. DELETE chunks WHERE doc_id IN (...) — batch-safe.      4. DELETE documents WHERE kb_id IN (...) — direct batch.    """    if not kb_ids:        return     for kb_batch_start in range(0, len(kb_ids), _BATCH_SIZE):        kb_batch = kb_ids[kb_batch_start : kb_batch_start + _BATCH_SIZE]        kb_placeholders = ",".join("?" * len(kb_batch))         # Step 1: collect doc_ids for this KB batch        doc_ids = [            row            for row in con.execute(                f"SELECT id FROM documents WHERE kb_id IN ({kb_placeholders})",                kb_batch,            )        ]         if doc_ids:            # Step 2: delete FTS rows in batches of _BATCH_SIZE            for doc_batch_start in range(0, len(doc_ids), _BATCH_SIZE):                doc_batch = doc_ids[doc_batch_start : doc_batch_start + _BATCH_SIZE]                doc_placeholders = ",".join("?" * len(doc_batch))                con.execute(                    f"""                    DELETE FROM chunk_fts                    WHERE rowid IN (                        SELECT id FROM chunks WHERE doc_id IN ({doc_placeholders})                    )                    """,                    doc_batch,                )                # Step 3: delete chunks                con.execute(                    f"DELETE FROM chunks WHERE doc_id IN ({doc_placeholders})",                    doc_batch,                )         # Step 4: delete documents for this KB batch        con.execute(            f"DELETE FROM documents WHERE kb_id IN ({kb_placeholders})",            kb_batch,        ) ``` > **Note:** Replace all calls to `clear_scope(con, kb_ids)` in `build_index()` with `_clear_scope_batch(con, kb_ids)`. --- ## I-05 / O-03: `stale()` Replacement — Full Delta Detection Replaces MAX(source_mtime) comparison with a three-set delta: added, deleted, modified. ```python def stale(args: argparse.Namespace) -> int:     if args.all_kbs:        kb_ids = list_kb_ids()        db_path = global_db_path()    else:        if not args.kb:            raise SystemExit("Either --kb <id> or --all-kbs must be provided.")        kb_ids = [args.kb]        db_path = per_kb_db_path(args.kb)     payload: dict = {        "status": "ok",        "db_path": str(db_path.relative_to(REPO_ROOT)),        "scope": "global" if args.all_kbs else args.kb,        "kb_status": [],    }     if not db_path.exists():        payload["status"] = "missing_index"        print(json.dumps(payload, indent=2))        return 1     con = connect(db_path)     for kb_id in kb_ids:        root = kb_dir_for(kb_id)         # Set A: files on disk  rel_path -> mtime        disk: dict[str, float] = {}        if root.exists():            for abs_path in iter_markdown_files(kb_id):                rel = str(abs_path.relative_to(root))                disk[rel] = abs_path.stat().st_mtime         # Set B: files in DB  rel_path -> source_mtime        indexed: dict[str, float] = {            row["rel_path"]: row["source_mtime"]            for row in con.execute(                "SELECT rel_path, source_mtime FROM documents WHERE kb_id = ?",                (kb_id,),            )        }         disk_set = set(disk.keys())        indexed_set = set(indexed.keys())         added = sorted(disk_set - indexed_set)        deleted = sorted(indexed_set - disk_set)        modified = sorted(            rel for rel in (disk_set & indexed_set)            if disk[rel] > indexed[rel]        )         is_stale = bool(added or deleted or modified)         payload["kb_status"].append(            {                "kb_id": kb_id,                "stale": is_stale,                "added": added,      # on disk, not in index                "deleted": deleted,  # in index, not on disk                "modified": modified,  # mtime advanced since last index            }        )     if any(item["stale"] for item in payload["kb_status"]):        payload["status"] = "stale"     print(json.dumps(payload, indent=2))    return 0 if payload["status"] == "ok" else 2 ``` --- # Phase 3 — Skill File Compliance Only `SKILL.md` was fully read. Remaining files assessed against patch pack content + SKILL.md references. [cite:23] ### `.claude/skills/apex-kb/SKILL.md` | check | result | |---|---| | `description_starts_with: "Use this skill when"` | ✅ PASS — confirmed in live file | | `sections_present: Skill Contract` | ✅ PASS | | `sections_present: Supporting Files` | ✅ PASS — YAML block format with `read_when` per file | | `sections_present: Procedure` | ✅ PASS | | `sections_present: Failure Modes` | ✅ PASS | | `sections_present: Output Requirements` | ⚠️ **PARTIAL** — `valid_outputs_by_mode` exists inside `completion_gate` YAML, but no standalone `# Output Requirements` heading | | `sections_present: Completion Gate` | ✅ PASS | | `procedure_step_count: between_5_and_8` | ✅ PASS — exactly 8 steps | | `supporting_files_format: YAML block with read_when` | ✅ PASS | | Retrieval commands documented | ❌ MISSING — no build-index/query/health/stale commands | **Corrective patch:** The "## Retrieval and Index Commands" block in the I-01 verified patch above resolves the missing retrieval surface. The `Output Requirements` heading gap is minor — `valid_outputs_by_mode` in the completion gate satisfies intent. Minimum corrective patch: ```markdown # Output Requirements See `completion_gate.valid_outputs_by_mode` in the Skill Contract block above. Each mode's output contract is defined there as the authoritative specification. ``` Insert between `# Failure Modes` and `# Completion Gate`. ### Remaining skill files (not read — compliance assumed from patch pack context) | file | compliance_status | basis | |---|---|---| | `references/kb-contract.md` | UNVERIFIED — read required | Not fetched | | `references/ingest-query-lint-audit-rules.md` | UNVERIFIED | Not fetched | | `references/script-command-contract.md` | UNVERIFIED | Not fetched | | `apex-plan/SKILL.md` | UNVERIFIED | Not fetched | | `apex-session/SKILL.md` | UNVERIFIED | Not fetched | | `PrecapNextDay/SKILL.md` | UNVERIFIED | Not fetched | --- # Final Patch Manifest | file | change_type | status | safe_to_apply | verified_against_live | |---|---|---|---|---| | `apex-meta/scripts/apex_kb_retrieval.py` | **CREATE** — new file | ready | ✅ yes — new file, no collision possible | N/A | | `.claude/skills/apex-kb/references/retrieval-contract.md` | **CREATE** — new file | ready | ✅ yes | N/A | | `.claude/skills/apex-kb/templates/query-output-template.md` | **CREATE** — new file | ready | ✅ yes | N/A | | `.claude/skills/apex-kb/SKILL.md` | **APPEND** — "Retrieval and Index Commands" section | ready | ✅ yes — no heading collision confirmed | ✅ REPO_VERIFIED | | `.claude/skills/apex-kb/SKILL.md` | **INSERT** — "# Output Requirements" heading | ready | ✅ yes | ✅ REPO_VERIFIED | | `.claude/skills/apex-kb/package-manifest.md` | **APPEND** — "## Retrieval assets" block | conditional | ⚠️ grep for "retrieval" first | ASSUMPTION_BASED | | `.claude/skills/apex-kb/references/kb-contract.md` | **APPEND** — canonical/derived + metadata fields | conditional | ⚠️ grep for "derived" and "domain" first | ASSUMPTION_BASED | | `.claude/skills/apex-kb/references/ingest-query-lint-audit-rules.md` | **APPEND** — retrieval/index/audit rules | conditional | ⚠️ grep for "build-index" and "FTS5" first | ASSUMPTION_BASED | | `.claude/skills/apex-kb/references/script-command-contract.md` | **APPEND** — retrieval command contract | conditional | ⚠️ grep for "apex_kb_retrieval" first | ASSUMPTION_BASED | | `apex-meta/scripts/apex_kb.py` | **DELEGATION HOOK** — read file first | **BLOCKED** | ❌ DO NOT apply until file is read | NOT READ | | `.gitignore` | **APPEND** — derived SQLite glob patterns | ready | ✅ yes — additive only | N/A | | `apex_kb_retrieval.py` → `build_index()` | **REPLACE** — add `--incremental` path | ready | ✅ standalone function, no external deps | N/A | | `apex_kb_retrieval.py` → `_clear_scope_batch()` | **REPLACE** `clear_scope()` | ready | ✅ schema-verified against patch pack | N/A | | `apex_kb_retrieval.py` → `stale()` | **REPLACE** — full delta detection | ready | ✅ standalone, backward-compatible JSON shape extended | N/A | > **One action required before applying 4 conditional patches:** fetch the 4 unread reference files (`kb-contract.md`, `ingest-query-lint-audit-rules.md`, `script-command-contract.md`, `package-manifest.md`) and grep for collision keywords listed above. All are confirmed to exist in the repo. [cite:22] The `apex-meta/scripts/apex_kb.py` path is **unconfirmed** — directory listing of `apex-meta/scripts/` must be run before any delegation hook is applied.``